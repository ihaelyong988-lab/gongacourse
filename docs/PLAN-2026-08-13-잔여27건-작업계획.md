# 잔여 27건 — 작업 결과 (마감)

- 계획 2026-08-13 21:20 · **착수·마감 2026-08-14 07:10~10:50 KST**
- 기준 커밋 `e716e51` → 결과는 아래 커밋·PR 참조
- 이 문서가 이 차수 잔여의 **단일 원천**이다. 처리한 항목은 여기서 해소로 옮겼다.

---

## 0. 한 줄 결론

# 27건 전량 종결. 그중 8건은 착수해 보니 이미 구현돼 있었다.

계획서는 전날 만든 것인데 **하루 만에 낡아 있었다.** 기수들이 「생존 대조」로 걸러 냈고, 재구현은 0건이다.

| | 건수 |
|---|---:|
| 실제로 고친 것 | 19 |
| 착수 시점에 이미 해소돼 있던 것 | 8 |
| 보류·이월 | 0 |

---

## 1. 항목별 결과

등급·축·편집 지점은 계획 시점의 값이다. 「이미 해소」는 현행 코드로 판정한 결과이며, 근거는 각 기수 반환값에 함수명·줄 번호로 남아 있다.

### 저장·홈·추천 (A1)

| id | 결과 | 무엇이 달라졌나 |
|---|---|---|
| G1-03/J5 | 고침 | 저장이 코스 전량 덤프에서 **시드 대비 델타**로 바뀌었다 — 실측 261,052B → 614B(0.24%) |
| G1-04/J5 | 이미 해소 | `DATA_VERSION` + `mergeVisitorRecords` + 부팅부 병합이 한 덩어리로 존재했다 |
| G1-23 | 이미 해소 | `sanitizeVisitorSettings` 가 화이트리스트·타입 검증 후 병합한다 |
| G1-21 | 이미 해소 | `gotoExploreTheme` 이 `setSearchKeyword("")` 로 상태와 DOM 을 함께 비운다 |
| G1-03/S1 | 고침 | 약속대로 홈은 앞 4개만 내고 나머지는 같은 줄의 'N개 더' 칩으로 그 자리에서 펼친다 |
| G1-04/J4 | 고침 | 거짓이던 'N개 우선 정렬 중' 확인줄을 걷고 **실제 맞춤 목록**을 그린다 |
| G1-16 | 이미 해소 | `pickBestCourse` 하나가 정본이고 홈·허브가 모두 위임한다 |
| G1-11 | 고침 | 카드 리본과 계절 중복 표기를 걷었다 |
| G22-24 | 고침 | 죽은 `allPhotos` 제거 · 본체(`renderMypage` 등급 스캔)는 A3 가 마무리 |

### 소통·후기·이스케이프 (A2)

| id | 결과 | 무엇이 달라졌나 |
|---|---|---|
| G3-07 | 고침 | **구멍이 실재했다** — 소통 피드(`feedItemHtml`)의 필명·본문·코스명이 무방비였다. 주입 전 INJECTED → 수정 후 4곳 전부 `null` |
| G3-09 | 고침 | 별점을 `radiogroup`/`radio` + roving tabindex + 화살표·Home/End 키 + 44×44px 로 바꿨다 |
| G3-16 | 고침 | 후기 영역 이모지·문자 별을 Font Awesome 으로 교체, 장식은 `aria-hidden` |
| G3-17 | 고침 | 피드 카드에 `role="button"`·키보드 조작, 정렬 탭에 `aria-pressed` |
| G22-03 | 이미 해소 | `canDelete` 가 uid 우선 + 옛 글은 필명 이중 판정, 필명 변경 시 옛 글에 uid 이관 |
| G22-18 | 이미 해소 | `isMine` 이 uid 전용이라 남의 이름을 넣어도 지표가 부풀지 않는다(217건 → 0건 실측) |
| G3-11 | 이미 해소 | `satisfactionOf` 는 표시용 순수 계산이고 되쓰는 곳이 0곳이다 |
| G3-18 | 고침 | `filterByKeyword`·도달 불가 `commKeyword` 분기·`topKeywords` 제거 |

### 헬스·만보기·모음집 (A3)

| id | 결과 | 무엇이 달라졌나 |
|---|---|---|
| G22-07 | 고침 | 만보기 본문만 갈아끼우도록 분리 — 시트 유지, 스크롤 693→693(대조군은 693→224 + 시트 닫힘) |
| G22-12 | 고침 | 완료 불가 미션 2건을 자가 점검 버튼으로 바꿨다(날짜별 저장, 자정 초기화) |
| G22-21 | 고침 | '측정 시작' 버튼이 실제로 측정을 시작한다(`motionActive=true` 실측) |
| G22-19 | 고침 | 토글을 `role="switch"` 버튼으로 바꾸고, 접근성 설정을 ⋮ 시트 밖 본문 카드로 꺼냈다 |
| G22-22 | 고침 | 두 세그먼트에 `role="tab"`·`aria-selected` · 만보기 쪽엔 `tablist` 자체를 신설 |
| G22-15 | 고침 | 잘못된 항목 진입 시 안내 + 돌아가는 길(0자 → 68자) |
| G22-16 | 고침 | 렌더 전 컨테이너를 비워 이전 모음집 잔존 0 |
| G22-17 | 고침 | 사진 실패 시 스트립을 지우지 않고 `sv-{season}` 채움으로 폴백 |
| G22-06 | 고침 | 검색어를 `escHtml` 로 감쌌다(속성 탈출 실측 차단) |
| G3-19 | 고침 | 문서 5곳의 코스 수를 실측값으로 정정 — **304개 · 5~20456줄**(id 1~304, 중복 0) |

### 루트 직접 처리

| 무엇 | 내용 |
|---|---|
| G3-17 잔여 | 탐색 필터 서랍에 `aria-expanded` |
| §4 위반 | 헬스 '내 후기 N' 카드 제거 — §4 는 등급·후기수 표기 자체를 금지한다 |
| 죽은 코드 | `renderCommKeywords`(무동작)·`segBtn`+`setSetting`(호출 0) 제거 |
| 터치 타깃 | 만보기 `.p2-menu` 38→44 · `.p2-nav` 34→44 · `.p2-share` min-height 44 |
| CSS | 별점 `focus-visible` 외곽선 · 맞춤 목록 마지막 행 헤어라인 · `.trek-stats` 4→3칸 |

---

## 2. 검증

`scripts/visitor-gate.js` **29/29 PASS**(기존 22 + 신설 7) · 가로 오버플로 0px · 콘솔 에러 0(favicon 404 제외 — `index.html` 에 아이콘 링크가 없어 브라우저 기본 요청이 나는 것) · `node --check` · `scripts/validate.mjs` PASS.

신설 게이트 7종: 저장이 후기·투표를 담는가 · 저장 payload 델타 유지 · 잘못된 모음집 진입 시 안내 · 별점 radiogroup · 별점 roving tabindex · 화살표 키로 별점 변경 · 필명 이스케이프.

**뮤테이션 증명 3종 통과.** 처방만 되돌려 목표 게이트가 **수치로** 죽는 것을 확인했다. 이 과정에서 **필명 이스케이프 게이트가 공허하게 통과하고 있던 것**을 잡았다 — 화면에 뜨는 이름은 `NICK` 이고 게이트는 `visitorSettings.nick` 만 바꿔, 렌더가 옛 값을 그려 아무것도 재지 못했다. 통과만 보고 넘겼으면 초록인 채로 구멍이 남았을 것이다.

---

## 3. 다음 차수로 남긴 것

「누가 손해를 보는가」를 적을 수 있는 것만 올린다.

| # | 무엇 | 누가·언제 | 근거 |
|---|---|---|---|
| 1 | **게이트가 홈 화면만 잰다** | 고객 · 지금 | 터치 타깃·대비 검사가 홈 기준이라, 만보기 화면의 38px·34px 버튼이 22/22 초록을 통과하고 있었다. 이번엔 손으로 찾았다 — 화면별로 재야 한다 |
| 2 | UI 이모지 잔존 28건 | 고객 · 지금 | 홈 섹션 라벨 ☀️🧭💬 · 타임라인 🚩🏁⛩️👣 · 효도추천 👴 등. 코드 자체가 "이모지는 금지이자 스크린리더 소음"이라 선언해 놓고 남아 있다 |
| 3 | 인라인 스타일 CSS 이관 | 아무도 · 발생 불가 | `button.mission`·`button.set-row`·`.dp-sv` 가 인라인이다. 동작은 정상이라 미관 항목이다 |

폴더 위생(스테일 worktree 1개)은 삭제 승인 대상이라 별건으로 남긴다.
