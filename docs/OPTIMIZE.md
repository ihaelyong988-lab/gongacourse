# OPTIMIZE — 꽁아코스 작업 절차 지침 + 개선 프롬프트
> 2026-07-02 제정 (4폴더 최적화 지시). 새 세션에서 **아래 프롬프트를 그대로 복붙**하면 재설명이 필요 없다.

## (a) 작업 절차 지침 — 표준 루프 (상세는 AGENTS.md)
1. **착수 전**: harness-context 훅이 규칙 자동 주입. UI 작업은 ui-ux-pro-max 선호출 + 시안 먼저.
2. **불변 규칙**: `defaultCourses` 304건 삭제 금지(§4) · 3-탭 아코디언 드로어 패턴 유지(§9 확정) · 빌드리스 바닐라 JS 유지(프레임워크 도입 금지).
3. **검증**: `node --check app.js` + Claude Preview 375px(python http.server 8765) 실화면 확인.
4. **마감**: 브랜치 커밋 → PR → GitHub Pages 배포(HTTPS — 만보기 센서 요건).
5. **성장 게이트(2026-07-02 신설)**: Stop 훅 `scripts/growth-gate.mjs` — 오늘 커밋이 있는데 AGENTS §9/CONVENTIONS 기록이 없으면 **마감 차단**.

## (b) 앱 개선 프롬프트 (복붙용)

### P1 ★우선 — 취향 검색 확장 (사찰·맛집·폭포 등 8종 취향 태그)
```
꽁아코스 탐색을 취향 태그 검색으로 확장해줘. docs/OPTIMIZE.md P1 기준.

[현황] app.js 테마 필터 4종(쉬운산책·맛집연계·사찰탐방·장거리)이 이미 있고, hasFood()/
hasTemple()(오탐 방지 TEMPLE_BAD 포함) 감지 패턴·FOOD_SYN 동의어 확장·REGION_MATCH LUT가
구현돼 있음(줄 번호는 탐색 힌트일 뿐 근거가 아니다 — 함수명으로 찾는다). 코스 304건의 timeline 텍스트가 태깅 원천.

[구현 — 기존 패턴 복제·확장, 데이터 수작업 0]
1. 감지 함수 추가(hasTemple 패턴 복제): hasWaterfall(폭포·瀑·소(沼)) · hasValley(계곡·협곡) ·
   hasSea(바다·해변·해안·섬) · hasLake(호수·저수지) · hasForest(숲·수목원·휴양림).
   각각 오탐 배제 토큰 목록 포함(예: "폭포수 커피" 같은 상호명 제외).
2. 테마 드로어 4종 → 취향 칩 8종(쉬운산책·맛집·사찰·폭포·계곡·바다·호수/숲·장거리)으로 확장,
   다중선택(AND 교집합) 지원. 기존 3-탭(지역|테마|계절) 드로어 패턴은 유지(§9).
3. 검색어 동의어 확장: FOOD_SYN 패턴 재사용해 WATERFALL_SYN(폭포/폭포수/瀑) 등 추가 —
   전역 검색창에서 "폭포"만 쳐도 해당 코스 전부 매칭.
4. 코스 카드에 취향 태그 뱃지(최대 2개) 표시 — 기존 카드 레이아웃(사진 좌 84px) 유지.
5. 각 필터 결과 0건이면 "조건 완화 제안" 문구(가까운 태그 1개 해제 제안).
6. 검증: node --check + 375px 프리뷰에서 8칩 각각 결과 수 확인 + 다중선택 교집합 확인.
```

### P2 — 취향 프로필 기억 (P1 후)
```
사용자가 자주 선택하는 취향 칩을 localStorage에 기록해 홈 "오늘의 추천"을 취향 가중치로
정렬해줘(날씨 추천 로직과 합산, applyPersonalSort 패턴 재사용).
```
