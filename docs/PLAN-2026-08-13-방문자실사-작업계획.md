# 방문자 관점 전수 실사 · 작업 계획서 — 꽁아코스

- 작성 2026-08-13 15:51 KST · 상태 **승인 대기(미착수)**
- 지시자 주인님 · 대상 `C:\dev\꽁아코스` (부수 1건: `C:\dev\cheonchaekbbang` 천책빵 질문 주기)
- 근거 규칙: 헌법 제3·7·8·9·10·11조 · `~/.claude/실행-결함-분석.md` §4~6 · 본 폴더 `AGENTS.md` §0~9
- 이 문서에 없는 작업은 하지 않는다(주인님 지시: 계획서 승인분 외 임의 판단 작업 금지).

---

## 0. PRE-FLIGHT (착수 게이트 6줄)

```
1. 규칙 조회 : 헌법 11개 조 · 실행-결함-분석 §4~6 · 꽁아코스 AGENTS.md §0~9(314줄) · CLAUDE.md · docs 5종 · memory 32건
2. 범위     : app.js 23,144줄(데이터 5~20,456 / 로직 20,457~23,144 = 2,687줄) · style.css 973 · index.html 93 · md 15개
             변경 성격: 조사 → 코드 수정 → 문서·폴더 정리
3. 충돌 지도 : 발굴·검증 = 읽기 전용, 충돌 0 → 병렬 가능
             수정 = app.js 단일 파일에 집중, 충돌 100% → 직렬 강제 (style.css·index.html은 별 파일이라 동시 가능)
             로컬 동시 실행 상한 = 2 (i7-7500U 물리 2코어 / 논리 4 → Workflow cap = min(16, 4-2) = 2)
             ∴ 소요는 Agent 수가 아니라 ⌈Agent÷2⌉ = Batch 회전 수로 계산
4. 소요     : 확정 구간 58~85분 + 변동 구간 71분(M=20 기준) = 129~156분
             착수 16:10 → 완료 18:19 ~ 18:46 KST
5. 승인     : 포괄 지시 확정(제11조 ①) · 본 계획서로 승인 요청 · 현재 미승인 · 정찰만 완료
6. 검증 계획 : 적대 검증 Agent 3기 + 회귀 18분 = 검증 예산 58%(게이트 기준 50% 이상 충족)
             기계 게이트 4종: node --check · scripts/validate.mjs · scripts/growth-gate.mjs · Playwright DOM 하드게이트
             내가 직접 읽을 diff: 치명·높음 수정 전량
```

---

## 1. 대상 확정 (제3조 — 만들기 전 1회 확정)

정찰 결과 지시 체크리스트의 상당수가 자매 앱(천책빵) 기능과 일치했다. 실측 대조표:

| 지시 항목 | 꽁아코스 | 천책빵 |
|---|---|---|
| 로그인 등록 / 로그아웃 | **없음** (프로필 간편등록만: 필명·휴대폰·사진) | 없음 (내 서재 버튼) |
| 관심사항 등록 | 동행(companion) 4종 · 취향 태그 8종 | 관심 분야 칩 |
| 방문자 맞춤설정 | `gongacourse_visitor_settings` | F4 맞춤설정 |
| 탭 닫기 팝업창 | 네이티브 `confirm("꽁아코스를 나갈까요?")` 1줄 (app.js:23129) | 전용 다이얼로그 `#exit-dialog` + 센티널 트랩 |
| 백링크 | 개념 없음 | 백링크 착지 분야 로직 존재 |
| 천책빵 질문 주기 | **0건(해당 없음)** | `app.js:405` 이번 주의 질문 — ISO 주차 씨앗, 주 1회 갱신 구현됨 |

**확정**: 지시 제목이 「꽁아코스 폴더의 앱」이므로 **꽁아코스가 주 대상**.
천책빵은 별 폴더라 파일 충돌 0 → 발굴 Batch의 빈 슬롯에서 **동시 처리, 추가 소요 0분**으로 흡수한다.

**선고지 사항**: 꽁아코스에는 로그인/로그아웃이 **존재하지 않는다.** 빌드리스 정적 SPA + localStorage 전용이
`AGENTS.md` §2 **불변 제약**이므로, 서버 인증 도입은 이 계획 밖(별도 승인)이다.
따라서 해당 항목은 **기능 부재**로 판정하고, 처방은 서버 없는 범위로만 낸다 —
신원 = 간편등록 강화 · 로그아웃 = 기기 내 세션·데이터 해제 경로.

---

## 2. AGENTS.md §16에 새길 세부 작업항목 (MECE 4군 11축)

> 규칙만 있고 게이트가 없으면 반복된다(제10조 ⑤). **모든 축에 기계 채점 게이트를 1개 이상 붙인다.**

### Ⅰ. 생애주기 축 — 방문자가 겪는 순서 그대로

**J1 진입 (첫 방문)**
- 점검: 캐시 0 상태 첫 렌더까지 시간 · 흰 화면 구간 · "무엇을 하는 앱인지" 첫 화면 전달력 · 데이터 0건(빈 저장·빈 후기) 화면 붕괴 여부
- 게이트: 콘솔 에러 0 · DOMContentLoaded→첫 카드 렌더 ms 기록 · 가로 오버플로 0px

**J2 신원 등록 (지시의 "로그인 등록")**
- 점검: 앱바 프로필 → 간편등록 모달 열림/취소/재편집 · 필명 미입력·공백·초장문·특수문자 · 휴대폰 형식 오류 · 사진 대용량/비이미지 파일 · 저장 후 앱바 즉시 반영
- 게이트: 저장 후 `gongacourse_visitor_settings` 실값 대조 · 새로고침 후 필명 유지 · 모달 ESC·포커스 트랩

**J3 관심사항 등록**
- 점검: 동행 4종 · 취향 태그 8종 다중선택 AND · 홈 바로가기 순서 설정 · 되돌리기 · 전체 해제 시 0건 화면
- 게이트: 선택값 → localStorage 값 1:1 · 필터 결과 0건일 때 빈 상태 안내 존재

**J4 맞춤 반영 (재방문 시 맞춤화면)**
- 점검: 오늘의 추천이 **현재 계절 풀에서만** 나오는가(AGENTS §4 절대규칙) · 날씨 조회 실패 시 계절만으로 폴백 · 관심사항이 실제 정렬·추천을 바꾸는가
- 게이트: 설정 전/후 추천 목록 diff ≥ 1건 · 계절 불일치 코스 0건

**J5 재방문 복원**
- 점검: 새로고침·탭 재개설 후 필명·사진·설정·저장목록 복원 · 구버전 스키마 키 잔존 시 내성 · localStorage 저장 거부(비공개 모드·용량 초과) 폴백
- 게이트: 저장소 차단 주입 후 콘솔 에러 0 · 화면 렌더 성공

**J6 이탈 (로그아웃 · 닫기 팝업)**
- 점검: 로그아웃 존부, 없으면 기기 내 데이터 해제 경로 존부 · 종료 팝업 발동 조건·문구·취소 동작 · 각 화면에서 뒤로가기 도착지 · 새로고침 후 위치
- 게이트: 홈 최상단 뒤로가기 = 팝업 1회 · 하위 화면 뒤로가기 = 팝업 0회 · 팝업 취소 후 화면 유지

### Ⅱ. 화면·연결 축

**S1 탭·화면 기능 완결성** — 탭 5종(홈·탐색·소통·헬스·만보기) + 화면 9종(상세·모음집·모음집상세·테마허브 포함). 각 화면 진입 · 핵심 조작 1건 · 빈 상태.
- 게이트: 화면별 필수 DOM 앵커 존재 · 콘솔 에러 0 · 빈 상태 문구 존재

**S2 백링크·왕복 무결성** — 모든 진입점(홈 바로가기 6 · 탭 5 · 카드 · 모음집 · 검색결과)에서 뒤로가기 도착지가 진입 직전 화면인가.
- 게이트: 진입점 × 뒤로가기 도착지 표 전량 일치(기계 대조)

### Ⅲ. 품질 축

**Q1 코드 결함·오류** — 콘솔 에러·미처리 예외 · 사용자 입력의 innerHTML 주입 · 네트워크 실패 폴백 · 중복 렌더/경합 · 타이머·리스너 누수
- 게이트: `node --check app.js` · `scripts/validate.mjs` · `scripts/growth-gate.mjs` · 콘솔 에러 0 · 입력 이스케이프 확인

**Q2 편의성·접근성·성능** — 터치 타깃 44px · 본문 대비 4.5:1 · aria/role · 폰트 3단 · `prefers-reduced-motion` · app.js 1MB 로딩 · 이미지 지연
- 게이트: computed style 실측 스크립트로 44px·4.5:1 위반 0건 · 전송량 기록

### Ⅳ. 위생 축

**H1 문서·코드베이스 위생** — md 15개 중복(스테일 worktree 사본 6개 포함) · 문서-코드 불일치 · 죽은 코드 · 앱 미참조 데이터 파일 · 빌드 타임 산물
- 게이트: md 중복 문자열 검출 0 · 문서에 적힌 줄 번호가 실제와 일치 · 앱 미참조 파일 목록 0
- 확인된 불일치 예: AGENTS §2·CLAUDE.md는 데이터 영역을 `1~17373줄`이라 적었으나 실제는 `5~20,456줄`

### 부수 축 (별 폴더)

**X1 천책빵 질문 주기** — `cheonchaekbbang/app.js:405` 이번 주의 질문. ISO 주차 씨앗으로 순서를 섞는 구조.
- 점검: 주가 바뀌면 첫 화면 문장이 **반드시** 바뀌는가 · 같은 주 재방문 시 묶음이 유지되는가 · 저장 거부 기기 폴백 · 문항 풀(`data/authored-questions.js`) 자체의 갱신 이력
- 게이트: 주차 키를 3주치 주입해 첫 문장 3개가 서로 다른지 기계 대조

---

## 3. 견적 산정 규칙 (신설 — 재발 차단 게이트)

> **오차 원인**: 1차 계획에서 작업량이 다른 단계에 Agent당 12~13분이라는 **같은 상수를 일괄로 곱했다.**

**신설 규칙 (AGENTS.md §16에 등재)**
1. 견적에는 **작업량 실측치**(줄 수 · 조작 수 · 편집 지점 수)와 **유도식**을 반드시 노출한다.
2. 단계에 상수를 일괄로 곱하는 산정을 금지한다.
3. 유도식 없이 시간을 적은 계획은 마감 차단 대상이다.
4. 소요는 Agent 수가 아니라 **⌈Agent÷2⌉ Batch 회전 수**로 계산한다(로컬 상한 2).
5. 편집 단계 견적은 **편집 지점 수 M의 함수**로 적는다. M은 적대 검증 이후에만 확정되므로, 그 전 단계는 확정 구간과 변동 구간을 분리 표기한다.

**1차 계획(5시간 43분) 대비 절감 분해**

| 오차 원천 | 크기 |
|---|---|
| 중복 작업 6건(회귀 2회 · 마감 2회 · 적대검증 과잉 · 라이브와 정적 이중 발굴 · 별도 보고 단계 · AGENTS 2회 집필) | 1시간 53분 |
| Agent당 상수 일괄 곱 | 약 50분 |
| 잔여 = 실제 작업 | 2시간 9분 ~ 2시간 36분 |

**제거한 중복·불필요 9건**

| # | 진단 | 처방 |
|---|---|---|
| 중복1 | 발굴 Agent(정적 독해)와 라이브 E2E가 같은 결함을 두 번 발굴 | 라이브를 **먼저** 돌려 증상 원장을 만들고 Agent 입력으로 주입 → Agent는 원인 규명 + 미발현 결함만 |
| 중복2 | 적대 검증이 라이브로 즉시 재현되는 결함까지 반증 | 재현 가능 항목은 Playwright 실측 즉결, Agent는 재현 불가(경합·폴백·예외 경로)만 → 6기→3기 |
| 중복3 | 회귀 검증 2회 | 회귀를 스크립트화해 1회 작성 후 재실행(1분) |
| 중복4 | 마감 2회 | 단일안 통합, 마감 1회 |
| 중복5 | "순번 확정+보고" 별도 단계 | 적대 검증 산출물이 곧 순번 목록 → 단계 삭제 |
| 중복6 | AGENTS.md 2회 집필 | 착수 전엔 축·게이트 정의만, 결함·처방은 마감 시 §9에 1회 |
| 불필요1 | style.css 973줄 전용 정독 Agent | 대비·터치타깃은 computed style 실측이 정답 → Agent 삭제, DOM 게이트로 대체 |
| 불필요2 | app.js 데이터 영역 5~20,456줄 독해 | 방문자 관점과 무관 → 대상에서 명시 제외(로직 2,687줄만) |
| 불필요3 | 천책빵 별도 단계 25분 | 별 폴더 = 충돌 0 → 발굴 Batch 빈 슬롯에 동시 편성, 추가 0분 |

---

## 4. 단계별 작업 계획 (착수 16:10 KST 기준)

### 확정 구간 — 발견 건수와 무관

| 단계 | 하는 일 | 작업량 실측 | 유도식 | Agent | Batch | 소요 | 누적 |
|---|---|---|---|---|---|---|---|
| **S1** | AGENTS.md §16 등재 — 11축 + 축별 게이트 + 견적 산정 규칙 | 1개 절 ≈ 90줄 | 내가 직접 작성 | 0 | — | 6분 | 16:16 |
| **S2** | **라이브 실사(내가 직접)** — Playwright로 방문자 6단계 완주 + 5탭 순회 + 백링크 왕복 + 종료 팝업 + 콘솔 수집 → **증상 원장** 산출 | 조작 42회 | 42×20초 + 판독 4분 | 0 | — | 18분 | 16:34 |
| **S3** | **결함 발굴 Workflow** — 증상 원장을 입력 주입 | 로직 2,687줄 + index.html 93 + 천책빵 질문 로직 120줄 → 5기 × 평균 560줄 | Batch 3 × 기당 8~15분 | **5** | 3 | 24~45분 | 16:58~17:19 |
| **S4** | **적대 검증 Workflow** — 라이브 재현 불가 항목만 반증. 다수결 미달은 기각 | 입력 = 발견 목록, 코드 재독은 국소 | Batch 2 × 기당 5~8분 | **3** | 2 | 10~16분 | 17:08~17:35 |

**S3 Agent 분할 (파일·줄 수 단위 — 의미 단위 금지)**

| 기 | 대상 | 줄 범위 |
|---|---|---|
| G1 | 내비·세션·이탈 — 라우터·visitorSettings·저장/복원·종료 confirm·앱바 + index.html | app.js 20,457~21,010 + index.html 전량 |
| G2 | 홈·테마허브·맞춤 반영 | app.js 21,010~21,570 |
| G3 | 탐색·상세·후기 | app.js 21,570~22,100 |
| G4 | 소통·헬스·만보기·모음집·전역 이벤트 | app.js 22,100~23,144 |
| G5 | **천책빵** 질문 주기(별 폴더, 충돌 0) | cheonchaekbbang/app.js 405~500 + data/authored-questions.js |

**확정 구간 합계 = 58~85분**

### 변동 구간 — app.js 편집 지점 수 **M**의 함수 (M은 S4 이후 확정)

| 단계 | 하는 일 | 유도식 | M=20 기준 |
|---|---|---|---|
| **S5** | **수정 Workflow** — 시급성 순차 ① 방문자 불편 ② 코드 결함·오류 ③ 편의성 | app.js 직렬 강제. 기당 상한 12지점 → M=20이면 2기 직렬 × 12분. style.css·index.html 1기는 동시라 추가 0분 | 24분 |
| **S6** | **회귀 검증** — 회귀 스크립트 작성·실행 + 라이브 재실사(DOM 하드게이트) + 정적 3종 | 8 + 2 + 8 | 18분 |
| **S7** | **위생 정리** — md 통폐합 1기 + 코드·찌꺼기 정리 1기(별 파일이라 동시) + 내 삭제 승인 검토 | Batch 1 × 8분 + 4분 | 12분 |
| **S8** | **마감** — 계획서 1:1 매칭 검증 → 커밋 → PR → **머지** → §9 기록 → 서버 정리 → 순번 보고 | 5 + 8 + 4 | 17분 |

**변동 구간 = 71분** (M ≤ 10 → −12분 · M ≥ 40 → +24분)

### 총계

**129~156분 = 2시간 9분 ~ 2시간 36분 · 완료 18:19 ~ 18:46 KST · Agent 15개 · Batch 9회**

- 남은 불확실 축은 **Agent 1기의 실지연 하나**뿐이다. S3 첫 Batch의 1기를 그대로 **캘리브레이션**으로 쓰고,
  실측이 나오는 즉시 남은 일정을 재고지한다. 별도 소요 0분.
- 약속한 완료시각은 체크포인트다. 끝났든 아니든 그 시각에 보고한다.

---

## 5. 중단 가능 지점

| 지점 | 시각 | 그때까지의 산출물 |
|---|---|---|
| S4 직후 | 17:08~17:35 | 개선사항 **순번 목록**(시급성 정렬) — 수정 착수 전 |
| S7 직전 | 18:00 전후 | 코드 수정 + 회귀 통과까지 — 폴더 정리 보류 |
| S8 직전 | 18:19~18:46 | PR 생성까지 — 머지 보류 |

---

## 6. 범위 밖 (양안 공통 제외)

- 서버 인증(로그인) 도입 — AGENTS §2 불변 제약(빌드리스·localStorage 전용) 위반
- 디자인 전면 개편 — 확정 스펙(3탭+수납장 아코디언 등) 변경 금지
- 콘텐츠 데이터 증설(242코스·폭포 100 등)
- `app.js` 데이터 영역 5~20,456줄의 내용 검수

---

## 7. S7 삭제 후보 (정찰 실측 — 승인 목록 그대로만 집행)

> 임의 삭제 금지. 파일별 1줄 근거를 붙여 S7 착수 전 재확인받는다.

| 대상 | 근거 |
|---|---|
| `.claude/worktrees/youthful-taussig-985cd2/` | git `prunable` 판정 스테일 worktree. md 6개 중복의 원천 |
| `parse_excel.py` · `convert_excel_to_js.py` | 빌드 타임 산물. index.html 미참조 |
| `excel_data.json` (201KB) | 위 스크립트의 중간 산물. 앱 미로드 |
| `가고픈곳 230307.xlsx` · `국내_꼭가봐야할_100대폭포_계절별_리서치.xlsx` (238KB) | 원본 엑셀. 앱 미참조 |
| `data/waterfall-visual-ledger.json` + `-part1~3.json` (424KB) | 시각 검증 원장. index.html 미로드 |
| `data/waterfall-visual-crossreview-part1~3.json` | 위와 동일 |
| `.codev/` → `.codex/` (미추적) | git 미추적 도구 캐시 |

※ 삭제 대신 보존이 나은 항목(원장·리서치 근거)은 `docs/` 또는 아카이브 이동을 먼저 제안한다.

---

## 8. 마감 조건 (자기점검 — 전부 "예"라야 마감)

1. PRE-FLIGHT 6줄을 첫 응답에 출력했는가
2. 약속한 완료시각에 (끝났든 아니든) 보고했는가
3. PLAN을 보고하고 승인받은 뒤 착수했는가
4. 적대 검증이 실제로 **도달**했는가 (계획만 세우고 못 간 것은 미도달)
5. 치명·높음 수정의 diff를 내가 직접 읽었는가
6. 이번에 겪은 오류를 AGENTS §9 원장에 남겼는가 (헌법 제8조)
7. 계획서 항목과 산출물을 **1:1로 매칭 검증**했는가 (누락 0)
8. 사과 문단 없이 마감했는가

---

## 9. 승인 요청

**「승인」 한 마디를 받으면 16:10에 S1부터 착수하고, 18:19~18:46에 머지까지 마감 보고한다.**
승인 없이는 읽기 전용 정찰 외 어떤 작업도 하지 않는다(제11조 ⑤).
