# AGENTS.md — <프로젝트명>

> 모든 AI 코딩/업무 도구가 작업 전 **가장 먼저 읽는 단일 기준 문서**.
> 여기 규칙·라우팅을 따르면 매번 재설명할 필요가 없습니다.
> 상세는 `docs/` 참조. 충돌 시 이 파일이 우선.

---

## 0. 자기개선 원칙 (Self-Improvement) — 최우선 · 모든 작업 공통 [이 섹션은 그대로 두세요]
> **핵심 규칙: 사용자가 한 번 설명한 작업 지시·요청 방식·선호는 두 번 다시 설명하게 하지 않는다.**
> 같은 지시를 2~3번 반복하게 만든다면, 그것은 하네스가 그 지시를 기록·적용하지 못한 **실패 신호**다.
> 한 번 받은 지시는 **그 자리에서 영구 규칙으로 굳혀**, 사용자가 다시 입력하는 수고를 없앤다.
> (업무 문서·리서치·앱 빌드·데이터 분석 — 작업 종류 무관하게 적용)

1. **첫 지시에 즉시 규칙화(그 턴 안에)**: 작업 명령·요청 방식·선호·교정·금지가 나오면 곧바로 적는다 —
   영구 규칙·작업 방식 → `docs/CONVENTIONS.md` · 절대 금지 → §4 · 도구·세션 공통 교훈 → `memory/`(전역) · 반복 절차 → `docs/WORKFLOWS.md` · 결정 맥락 → §9.
2. **재설명 요구 금지**: 이미 기록된 지시는 다시 묻지 않고, 사용자가 다시 설명하게 하지 않는다. 새 작업 전 §6 라우팅·CONVENTIONS·memory를 **먼저 조회**해 적용한다.
3. **절차 승격**: 같은 절차를 2회 이상 하면 `docs/WORKFLOWS.md` 등재 → 안정되면 스킬화.
4. **마감 자기점검**: 마감(§8)에서 "이번에 사용자가 설명한 지시·방식·교정"을 규칙으로 남겼는지 확인. 안 남기면 **다음에 또 같은 설명을 요구하게 됨** = §0 위반.
5. **활성 훅(자동 강제)**: `.claude/settings.json`의 UserPromptSubmit 훅이 매 입력에 본 원칙·라우팅을 주입.

---

## 1. 프로젝트 한 줄 정의
**꽁아코스** — 경험자 후기로 나들이 코스+맛집을 쉽게 찾는 **휴대폰 전용** 가이드/소통 앱. 산출물: 빌드 없는 정적 SPA(index.html + style.css + app.js).

## 2. 아키텍처 / 제약 (불변 — 깨지 말 것)
- **휴대폰 우선**: 데스크톱 2단 대시보드 금지. 화면(screen) 단위 단일 컬럼 + 하단 5탭바.
- **빌드리스**: 번들러·프레임워크 없음. 바닐라 JS, Font Awesome + Noto Sans KR(CDN)만 허용.
- **데이터 보존**: `app.js`의 `defaultCourses`(242개 코스, 1~17373줄)는 절대 손실 금지. 사용자 데이터는 localStorage(`gongacourse_data`, `gongacourse_saved`, `gongacourse_visitor_settings`).
- **내비게이션은 history 기반 화면 스택**: `navigate()`가 pushState, `popstate`가 뒤로가기 처리. 종료 confirm은 홈 최상단에서만.

## 3. 구조 지도
- `index.html` — 앱바 + 6개 `<section class="screen">`(home/explore/detail/community/saved/mypage) + 하단 `.tabbar`. 각 screen은 빈 `#*-body` 컨테이너, JS가 채움.
- `app.js` 로직(17374줄~): 라우터(`showScreen`/`navigate`/`goTab`/`goBack`) · 렌더러(`renderHome`/`renderExplore`/`renderDetail`/`renderCommunity`/`renderSaved`/`renderMypage`) · 필터(`getFilteredCourses`/칩 setter) · 후기/투표/사진(`submitComment`/`castVote`/`handlePhotoSelected`) · 설정(`setSetting`/`setFontSize`/`toggleContrast`).
- 코스 스키마: `{id,title,season,seasonName,location,duration,difficulty,type,satisfaction,votesUp,votesDown,timeline[{spot,desc,time}],comments[{user,text,date,ratings{scenery,path,parking}}],photos[],foods[]}`.
- 검증/미리보기: `.claude/launch.json`의 `gonga`(python http.server 8765) → preview, 모바일 375px.

## 4. 절대 규칙 (DON'T)
- ❌ **추천 코스를 현재 계절과 무관하게 노출 금지.** 오늘의 추천은 반드시 `getCurrentSeason()`(월 기준) 풀에서만 뽑고, 가능하면 실시간 날씨(`weatherInfo`)로 보정. (한여름에 겨울코스 = 앱 존재이유 상실)
- ❌ 외부 API는 무료·키불필요·CORS허용만(현재 날씨 = Open-Meteo). 실패/거부 시 반드시 graceful 폴백(계절만).
- ❌ 코스 카드 이미지에 **일러스트·이모지 금지 → 실사만**. 탐색 필터를 "모든 칩 한 줄 노출"로 되돌리기 금지 → **3탭+수납장(서랍) 아코디언** 고정(§9 2026-06-28 확정 스펙).
- ❌ 파괴적 git(reset --hard·force push) 사용자 승인 없이 금지. 기본 브랜치 직접 커밋·푸시 금지(브랜치 사용).

## 5. 작업 후 검증 (필수)
- `node --check app.js` + Claude Preview(`.claude/launch.json`의 `gonga`, 375px)로 **실제 화면 확인**. preview serverId는 **잘리지 않은 전체 문자열** 사용(부분 id는 eval만 맞고 screenshot은 stale).
- 콘솔 에러 0 확인. 스크린샷이 stale이면 stop→start 재기동 또는 eval로 DOM 직접 검증.

## 6. 스킬·도구 라우팅 (작업 → 도구, 재질문·우회 금지)
| 작업 | 스킬/도구 (반드시 사용) |
|---|---|
| **UI/UX 디자인·레이아웃·스타일·개선·리뷰** | **`ui-ux-pro-max` 스킬 호출**(수동 추정 금지). 디자인시스템/팔레트/UX 가이드 조회 후 적용 |
| 화면 미리보기·검증 | Claude Preview MCP (`preview_start` gonga → resize 375 → screenshot/eval) |
| 코스 이미지 | **실사만**(키워드 매칭 loremflickr). 일러스트·이모지 금지 |
| 추천 코스 | 현재 계절(`getCurrentSeason`) + 날씨(`weatherInfo`) 연동 필수 |
| 새 폴더 하네스 | `/harness-setup` |
| 지식그래프 | `/graphify` |

## 7. 워크플로우 라우팅
상세는 `docs/WORKFLOWS.md`.

## 8. 마감 루틴 (Closeout)
1. 검증 통과  2. (선택)인덱스/그래프 갱신  3. 교훈 `memory/` 기록  4. 브랜치 커밋(기본 브랜치 직접/푸시는 승인 시)  5. 변경 요약 보고

## 9. 작업 이력 · 지시 맥락 (Decision Log) — append-only
### 2026-06-28 — 모바일 네이티브 전면 재구축 (v2.0)
> 사용자 불만: 뒤로가기로 정보 못 찾음 / 코스 그래픽 허접·화면 벗어남 / 맞춤설정 뭘 맞추는지 모름 / 코스탐색 필터 작동 안 함 / 홈 작동 안 함 / 소통 채널 컨셉 부재. 앱 정체성 = "경험자와 소통하는 가이드북".
1. **변경**: 데스크톱 2단 대시보드 → 화면 스택 + 하단 5탭(홈/탐색/소통/저장/내정보)로 전면 재구축. ①뒤로가기=화면 복귀(history 라우터, 종료 confirm은 홈에서만) ②가짜 SVG 지도 → 코스별 실제 세로 타임라인 + 카카오맵 딥링크 ③맞춤설정=결과 즉시 시각화("효도코스 N개 우선") + 내정보로 흡수, 개발자 엑셀도구 제거 ④`<select>` → 칩 필터(지역/테마/계절) ⑤추천 홈 신설(오늘의 추천1+상황별 바로가기+맞춤배너) ⑥소통 탭 신설(전체 후기 피드+인증샷 갤러리).
2. **원인/교훈**: 근본원인은 "PC 대시보드를 폰에 욱여넣음". 데이터(242코스)는 그대로 두고 화면구조·내비·시각화만 폰 네이티브로 교체. 결정사항: 홈 = 오늘추천1+상황바로가기+맞춤배너(후기피드는 소통탭). 식당 전화는 가짜(010-1234)라 제거 → 카카오맵 링크로 대체.
3. **검증**: node --check 통과, preview 375px에서 6화면+뒤로가기+칩필터+맞춤배너 전부 정상, 콘솔 에러 0. 브랜치 `redesign/mobile-native` (커밋 보류 — 사용자 승인 시).

### 2026-06-28 — 홈 첫화면 세로 공간 압축
> 사용자 지시: "첫화면을 작업전 형식으로(=원래 리스트처럼 빽빽하게) 세로폭 공간 효율화". 선호 규칙: **홈은 한 화면에 최대한 많은 정보, 큰 히어로·큰 버튼으로 세로 낭비 금지**.
1. **변경**: 오늘의 추천 카드 압축(큰 "코스 자세히 보기" CTA 제거→카드 전체 탭+우상단 화살표, loc·시간·만족·맛집을 메타 1줄로 통합) · 맞춤배너→슬림 1줄 바(`match-bar`) · 상황별 바로가기 2줄 그리드(`sit-grid`)→가로 스크롤 1줄(`sit-scroll`/`sit-chip`) · 섹션라벨 여백 축소(`home-label`, 더보기 링크 추가) · 후기 2→3개. 결과: 오늘추천+맞춤바+상황+후기3 전부 한 화면.
2. **교훈**: 폰 홈은 "빽빽한 정보 밀도"가 선호. 가로 스크롤로 행 수를 줄여 세로 절약. 이 밀도 선호를 향후 다른 화면에도 적용.
3. **검증**: preview 375px 스크롤 없이 4섹션 노출 확인, 카드 클릭→상세 정상, 콘솔 에러 0. (주의: preview 렌더러가 멈추면 stop→start로 재기동.)

### 2026-06-28 — 오늘의 추천 계절·날씨 연동
> 사용자 지시: "추천 코스를 현재 계절·날씨와 연동. 한여름에 겨울 코스 뜨면 앱이 왜 필요한가." → §4 규칙으로 승격.
1. **변경**: `pickTodayCourse()`를 `getCurrentSeason()`(월→계절) 풀로 강제. 실시간 날씨(`loadWeather`, Open-Meteo + geolocation, 키 불필요)로 보정 — 비/눈=짧고 쉬운 코스, 무더위=숲·계곡, 추위=짧은 코스. 홈 라벨에 "🌿 여름 · 32° ☀️" 컨텍스트 칩 + 보정 시 안내 문구. 날씨 1h 캐시(localStorage `gongacourse_weather`).
2. **교훈**: 위치 거부/오프라인이면 계절-only로 자동 폴백(필수). 날씨는 비동기 → 로드 후 홈 재렌더.
3. **검증**: 6월=여름 코스 노출 확인(겨울코스 사라짐), 위치거부 폴백·날씨주입(hot) 양쪽 정상, 콘솔 에러 0.

### 2026-06-28 — 카드 가로형 미디어 레이아웃 + 계절 CSS 비주얼
> 사용자 지시: "도형+내부 이미지+설명문구 재배치로 세로 효율 개선(전문가 기법)". 확정: 대상=홈+코스카드, 이미지=CSS 생성(오프라인 안전).
1. **변경**: ①`.season-visual`+`.sv-{season}` — 계절별 그라데이션 + 산 실루엣(인라인 SVG data URI) + 태양 + 이모지로 **사진 없이 도형 내부 비주얼** 생성(외부 의존 0). ②`courseCardHtml` 세로 스택 → **가로형 미디어 카드**(비주얼 74px 좌 · 문구 우, `cc-visual`/`cc-main`). ③홈 '오늘의 추천' → 비주얼 104px 좌 + "추천" 리본 + 문구 우(`tc-row`/`tc-visual`/`tc-content`).
2. **교훈**: 세로 효율의 정석 = **세로 스택을 좌우 미디어 카드로 묶기**(높이↓, 한 화면 밀도↑) + 사진 없으면 CSS 그라데이션/SVG 실루엣으로 비주얼 대체(오프라인 §4 준수). 텍스트는 ellipsis로 1줄 클램프. 이 미디어카드 패턴을 향후 신규 카드 기본형으로.
3. **검증**: preview 375px 홈 여름비주얼·탐색 겨울비주얼 카드 정상, 가로 오버플로우 0, 콘솔 에러 0.

### 2026-06-28 — 실사 사진 전환 + 탐색 검색/필터 재배치 (일러스트 폐기)
> 사용자 강한 피드백: "그림(일러스트) 넣으면 장난하는 줄 안다 → **실사**여야 실감난다." + 탐색=맞춤검색창이니 "검색창을 최상단 로고 옆으로, 지역·테마·계절 필터를 한 줄로, 리소스(결과) 최대화".
1. **변경**: ①`season-visual` 일러스트(이모지+산 SVG) **폐기** → `coursePhoto()` 키워드 매칭 **실사**(loremflickr, 계절/사찰/바다 키워드, `lock=id`로 고정). `<img onerror>` 실패 시 `sv-{season}` 그라데이션 폴백. ②검색창을 앱바(로고 옆 최상단)로 이동(`onGlobalSearch`, 입력 시 탐색으로 라우팅) — 탐색 내 검색줄 제거로 세로 절약. ③탐색 필터: 3줄 라벨형 → **한 줄 가로 스크롤 필터 바**(`filter-bar`, 지역|테마|계절 구분), sticky. ④카드 비주얼 84px 실사.
2. **교훈(영구 선호)**: **코스 이미지는 반드시 실사**(일러스트·이모지 금지). 외부 이미지는 키워드 매칭 + onerror 폴백 필수. 탐색=검색 우선·필터 1줄·결과 최대화. ※초기에 CSS 일러스트를 권장했다가 반려됨 — 다음부터 이미지는 실사 우선 제안.
3. **검증**: preview 375px 실사 사진(산·숲·사찰) 정상, 검색창 로고 옆, 한 줄 필터(서울 7개) 정상, 콘솔 에러 0. ※loremflickr 지연/불안정 가능 → 폴백 유지, 추후 정식 이미지 호스팅 검토.

### 2026-06-28 — 탐색 필터 = 3탭 + 수납장(서랍) 패턴 [확정 스펙·변경 금지]
> 사용자 재지시(불만): "한 줄로 나열"의 의미 = 모든 칩을 한 줄에 펼치는 게 **아니라**, **지역·테마·계절 3개 탭을 한 줄에 두고 → 각 탭을 누르면 수납장(서랍)처럼 세부 항목이 펼쳐지는** 아코디언. 같은 설명 반복하게 만든 것 = §0 위반.
1. **변경(확정 UI)**: `renderExploreFilters()` — `ftab-row`에 지역/테마/계절 3개 `.ftab`(각 "라벨+현재값+▾"). 탭 클릭=`toggleDrawer(group)` → 그 그룹의 `.filter-drawer`(칩 묶음)만 아래로 펼침(아코디언, 동시에 1개만). 항목 선택=`setRegion/Theme/Season` → 서랍 닫고 `renderExploreFilters()`+`renderExploreList()`. 값 선택 시 탭은 `has`(초록) + 우측 `ftab-clear`(↺) 노출. **서랍 토글은 리스트 재렌더 안 함**(이미지 깜빡임 방지).
2. **교훈(영구)**: 필터 UI는 **3탭+서랍(수납장)** 고정 — 모든 칩 한 줄 노출 방식으로 되돌리지 말 것. 사용자가 "수납장/서랍/펼침"을 말하면 아코디언 disclosure를 뜻함. **새 작업 전 본 §9·§4를 먼저 읽어 같은 지시 반복 금지(§0).**
3. **검증**: 지역 탭→서랍 펼침→서울 선택→탭 "지역 서울"·7개 필터·초기화 노출, 콘솔 에러 0.

### 2026-06-28 — 소통 탭: 상단 업로드 컴포저 + 하단 후기 필터 탭
> 사용자 지시: 소통을 사용자 관점으로 — "상단에 사진/카메라로 쉽게 업로드, 하단 방문후기는 최신순·평점순·검색 탭으로 필터링".
1. **변경**: ①**상단 컴포저**(`composer`): 텍스트 + 코스명(선택) + 카메라 버튼(`capture=environment`) + 올리기. 자유 게시물은 코스에 묶이지 않고 별도 저장소 `communityPosts`(localStorage `gongacourse_posts`)에 저장. ②**하단 피드**: `communityFeed()`가 자유게시물 + 전체 코스 후기를 통합 정규화. **정렬 탭** `최신순|평점순|검색`(`commSort`) — 평점순=후기 ratings 평균 내림차순, 검색=검색창 노출 후 텍스트/작성자/코스명 필터. ③피드 카드에 별점·사진·코스태그 표시, 본인 자유글은 삭제 가능. 게시 후 `commSort='latest'`로 리셋해 새 글 상단 노출.
2. **교훈**: 데이터가 코스 종속이라 자유 업로드는 별도 `communityPosts` 저장소로 분리(코스 후기와 피드에서 통합 표시). 정렬/검색은 리스트만 재렌더(이미지 깜빡임 방지). 카메라는 `<input capture>`로 모바일 직촬 지원.
3. **검증**: preview 375px 컴포저 게시(피드 최상단+영속), 최신/평점(4.7 우선)/검색 탭 전환·필터 정상, 콘솔 에러 0. ※preview는 반드시 현재 serverId 사용(옛 id는 stale 페이지).

### 2026-06-28 — 소통 정정: "검색순"=인기검색 정렬 + 컴포저 간소화 [용어 확정]
> 사용자 정정(중요): **"검색순" = 검색창이 아니라 "많이 검색된 키워드(인기) 순 정렬"**. 컴포저는 "사진/갤러리 + 간단의견 + 올리기"로 간소화·직관화(코스명 등 군더더기 제거).
1. **변경**: ①정렬 탭 `검색`(검색창)→**`검색순`(popular)**: 앱바 검색 시 `searchCounts`(localStorage `gongacourse_searchcounts`) 누적(`bumpSearch`, 900ms 디바운스) → 검색순은 `itemPopularity`(코스 votesUp+댓글*3+만족도 + 검색어매칭*20)로 정렬. 상단에 **인기 키워드 칩**(`topKeywords`, `#키워드 횟수`) 노출, 칩 탭=그 키워드 후기만(`commKeyword`). 빈 상태=힌트. ②컴포저 간소화: 코스명 입력 제거, **간단의견 1줄 + [사진(카메라 capture)]·[갤러리(capture 해제)]·[올리기]** 3버튼 균등 배치(`composer-link`/`composer-post`). ③소통 검색창(`comm-search`)·`onCommSearch`·`commSearch` 전면 제거.
2. **교훈(용어 확정)**: 사용자 어휘 **"검색순"=인기/트렌딩 정렬**(검색박스 아님). "탭링크"=눈에 띄는 버튼. "간소화/직관적"=필드 최소화+큰 버튼. preview **serverId는 잘리지 않은 전체 문자열** 사용(부분 id는 eval은 fuzzy매치되나 screenshot은 stale 프레임 → 오판 주의).
3. **검증**: 검색어 주입(북한산5·단풍3)→칩 `#북한산 5 #단풍 3`, 북한산 코스 후기 인기도 최상단, 칩필터 2건 정확, 콘솔 에러 0(eval 검증).

### 2026-06-28 — "사각도형나라" 탈출: 수평 라인 밴딩 + 박스 약화 (ui-ux-pro-max 스킬 적용)
> 사용자 지시: 화면이 사각 박스만 쌓여 단조로움 → "수평라인으로 살짝 포인트, 안정감 있게 박스나라 탈출". + UI/UX 스킬 썼냐는 지적. → 이번부터 `ui-ux-pro-max` 스킬 실제 호출해 적용(이전엔 수동 원칙만).
1. **변경**: ①`sectionLabel()` 헬퍼 — 섹션 제목 + **수평 hairline(그라데이션)** + 우측요소. 홈 3개 섹션·소통 "방문후기"에 적용. ②피드를 **박스→hairline 행**(`.feed-item`/`.feed-card` border 제거, `border-bottom: var(--hair)`)으로 = 박스나라 탈출 핵심. ③카드 제목(`d-card-title`/`set-title`)에 **하단 hairline** 밴딩. ④카드 테두리 `--border`→**`--hair`(연하게)**로 약화(course/today/d-card/set-card/composer). ⑤anti-pattern(AI 보라/핑크) 회피 → 맞춤배너 **보라→그린**. 토큰 추가 `--hair #edf0ed`, `--accent-line #cfe0d2`.
2. **교훈(영구 디자인 원칙)**: 박스 단조로움 탈출 = **세로 스택 박스 줄이고 수평 hairline으로 밴딩**(섹션 헤더 라인 + 리스트는 박스 대신 구분선 행). 카드 테두리는 `--hair`로 은은하게. **UI/UX 작업은 `ui-ux-pro-max` 스킬을 호출해 적용**(수동 추정 금지). 보라/핑크 그라데이션 회피, 그린 자연 팔레트 유지.
3. **검증**: preview 375px 홈(섹션 라인+그린 배너+hairline 후기)·소통(방문후기 라인+hairline 피드)·상세(카드 제목 라인) 정상, 콘솔 에러 0.

### 2026-06-28 — 하네스 규칙 강제주입 훅 + 내정보=만보기 계기판
> ① 사용자 분노: md에 적어도 우회됨 → 원인=UserPromptSubmit 훅이 고정문구만 주입(실제 규칙 X). ② 내정보에서 효도코스(맞춤설정)·오프라인·앱정보 삭제, 만보기 계기판+기록 신설.
1. **변경**: ①`scripts/harness-context.mjs` — AGENTS.md §4/§6/§9를 매 턴 컨텍스트 강제주입(우회 차단). settings.json 배선은 **사용자 승인 대기**(자가수정 가드 차단). ②mypage: 동반자/교통 카드·오프라인·앱정보 제거 → **만보기 대시보드**(SVG 원형 게이지 `ped-gauge` + km/kcal/목표 + 목표칩 + 걸음기록 input·+칩 + 7일 막대). 저장소 `gongacourse_steps {goal,days}`. ③동반자 개인화는 **홈 배너로 이동**(`match-card` 칩 `setCompanionHome`)해 끊김 방지. 화면 접근성은 mypage 유지.
2. **교훈**: 규칙은 기록만으론 강제력 0 → **훅이 실제 규칙을 주입해야** 적용됨. 만보기는 센서 없이 기록형(localStorage). 설정 삭제 시 의존 기능(홈 배너) 이전 필수.
3. **검증**: 6,420보→4.49km·257kcal·게이지80%(offset65.3), 주간바7·오늘80%, 삭제3종 제거 확인, 홈 동반자칩→효도코스 결과, 콘솔 에러 0(eval).

### 2026-06-28 — 만보기 실센서 측정(DeviceMotion) + 내정보 콤팩트 대시보드
> 사용자: 내정보 상단 박스 콤팩트 정돈, 만보기를 대시보드로 한 화면에, "폰에서 켜고 움직이면 카운트되는지" 확인.
1. **변경**: ①프로필 가로 1줄(`profile-row`/`avatar-sm`)·스탯 압축. ②만보기 대시보드화: 게이지(132px)+측정버튼+km/kcal+목표칩을 `ped-main` 2단 배치, 주간바·빠른칩 압축 → 한 화면. ③**실시간 걸음 측정**: `DeviceMotion`(가속도계) 피크검출(`onDeviceMotion`, 저주파분리 dyn>1.2, 간격 280ms, iOS `requestPermission`). `countOneStep`→`updateGaugeLive`(전체 재렌더 없이 게이지/숫자 즉시 갱신, 10보마다 저장). `측정 시작/정지` 토글.
2. **교훈**: 웹 만보기는 DeviceMotion으로 실제 카운트 가능하나 **실기기+HTTPS+동작권한** 필수(PC 프리뷰는 센서 없어 미작동 → eval로 합성이벤트 검증). 라이브 카운트는 DOM 직접 갱신(재렌더 금지).
3. **검증(eval 합성)**: 걷기 12회→12보 카운트+게이지 라이브갱신, 노이즈 0, 간격가드 중복 0, 콘솔 에러 0.
