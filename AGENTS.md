# AGENTS.md — 꽁아코스 (휴대폰 전용 트레킹/나들이 가이드 웹앱)

> 모든 AI 코딩/업무 도구가 작업 전 **가장 먼저 읽는 단일 기준 문서**.
> 여기 규칙·라우팅을 따르면 매번 재설명할 필요가 없습니다.
> 상세는 `docs/` 참조. 충돌 시 이 파일이 우선.

---

## 0. 자기개선 원칙 (Self-Improvement) — 최우선 · 모든 작업 공통 [이 섹션은 그대로 두세요]
> **핵심 규칙: 사용자가 한 번 설명한 작업 지시·요청 방식·선호는 두 번 다시 설명하게 하지 않는다.**
> 같은 지시를 2~3번 반복하게 만든다면, 그것은 하네스가 그 지시를 기록·적용하지 못한 **실패 신호**다.
> 한 번 받은 지시는 **그 자리에서 영구 규칙으로 굳혀**, 사용자가 다시 입력하는 수고를 없앤다.
> (업무 문서·리서치·앱 빌드·데이터 분석 — 작업 종류 무관하게 적용)

1. **첫 지시에 즉시 규칙화(그 턴 안에)**: 작업 명령·요청 방식·선호·교정·금지가 나오면 곧바로 적는다 —
   영구 규칙·작업 방식 → `docs/CONVENTIONS.md` · 절대 금지 → §4 · 도구·세션 공통 교훈 → `memory/`(전역) · 반복 절차 → `docs/WORKFLOWS.md` · 결정 맥락 → §9.
2. **재설명 요구 금지**: 이미 기록된 지시는 다시 묻지 않고, 사용자가 다시 설명하게 하지 않는다. 새 작업 전 §6 라우팅·CONVENTIONS·memory를 **먼저 조회**해 적용한다.
3. **절차 승격**: 같은 절차를 2회 이상 하면 `docs/WORKFLOWS.md` 등재 → 안정되면 스킬화.
4. **마감 자기점검**: 마감(§8)에서 "이번에 사용자가 설명한 지시·방식·교정"을 규칙으로 남겼는지 확인. 안 남기면 **다음에 또 같은 설명을 요구하게 됨** = §0 위반.
5. **활성 훅(자동 강제)**: `.claude/settings.json`의 UserPromptSubmit 훅이 매 입력에 본 원칙·라우팅을 주입.

---

## 1. 프로젝트 한 줄 정의
**꽁아코스** — 경험자 후기로 나들이 코스+맛집을 쉽게 찾는 **휴대폰 전용** 가이드/소통 앱. 산출물: 빌드 없는 정적 SPA(index.html + style.css + app.js).

## 2. 아키텍처 / 제약 (불변 — 깨지 말 것)
- **휴대폰 우선**: 데스크톱 2단 대시보드 금지. 화면(screen) 단위 단일 컬럼 + 하단 5탭바.
- **빌드리스**: 번들러·프레임워크 없음. 바닐라 JS, Font Awesome + Noto Sans KR(CDN)만 허용.
- **데이터 보존**: `app.js`의 `defaultCourses`(**304개 코스, 5~20456줄** — 2026-08-14 실측)는 절대 손실 금지. 사용자 데이터는 localStorage(`gongacourse_data`, `gongacourse_saved`, `gongacourse_visitor_settings`).
- **내비게이션은 history 기반 화면 스택**: `navigate()`가 pushState, `popstate`가 뒤로가기 처리. 종료 confirm은 홈 최상단에서만.

## 3. 구조 지도
- `index.html` — 앱바 + 6개 `<section class="screen">`(home/explore/detail/community/saved/mypage) + 하단 `.tabbar`. 각 screen은 빈 `#*-body` 컨테이너, JS가 채움.
- `app.js` 로직(17374줄~): 라우터(`showScreen`/`navigate`/`goTab`/`goBack`) · 렌더러(`renderHome`/`renderExplore`/`renderDetail`/`renderCommunity`/`renderSaved`/`renderMypage`) · 필터(`getFilteredCourses`/칩 setter) · 후기/투표/사진(`submitComment`/`castVote`/`handlePhotoSelected`) · 설정(`setSetting`/`setFontSize`/`toggleContrast`).
- 코스 스키마: `{id,title,season,seasonName,location,duration,difficulty,type,satisfaction,votesUp,votesDown,timeline[{spot,desc,time}],comments[{user,text,date,ratings{scenery,path,parking}}],photos[],foods[]}`.
- 검증/미리보기: `.claude/launch.json`의 `gonga`(python http.server 8765) → preview, 모바일 375px.

## 4. 절대 규칙 (DON'T)
- ❌ **추천 코스를 현재 계절과 무관하게 노출 금지.** 오늘의 추천은 반드시 `getCurrentSeason()`(월 기준) 풀에서만 뽑고, 가능하면 실시간 날씨(`weatherInfo`)로 보정. (한여름에 겨울코스 = 앱 존재이유 상실)
- ❌ 외부 API는 무료·키불필요·CORS허용만(현재 날씨 = Open-Meteo). 실패/거부 시 반드시 graceful 폴백(계절만).
- ❌ 코스 카드 이미지에 **일러스트·이모지 금지 → 실사만**. 탐색 필터를 "모든 칩 한 줄 노출"로 되돌리기 금지 → **3탭+수납장(서랍) 아코디언** 고정(§9 2026-06-28 확정 스펙).
- ❌ **코스 이미지 키워드는 제목의 지형(섬/바다/호수/계곡/숲/공원/산)을 사찰보다 우선**(`photoKeyForCourse`). 동선에 사찰이 끼어도 "삼형제섬"엔 바다 사진. temple 키는 **제목 자체가 사찰일 때만**.
- ❌ **사찰 판별에 단독 '사/절/암' 문자 매칭 금지**(식사·행사·계절 오탐 → 필터 224/242 폭주). 반드시 `spotIsTemple()`(사찰 전용어 + '○○사/○○암' 토큰, 비사찰어 제외) 사용.
- ❌ **취향 태그(폭포/계곡/바다/호수/숲) 판별에 단독 '섬/항/소' 등 광범위 문자 `includes` 매칭 금지** — 반드시 `courseHasTag()`(토큰 단위 WORD 전용어 + BAD 오탐 배제: 상호명·공항/운항·섬진강/무섬 등) 경유. 접미 토큰은 `○○섬(길|투어|여행)?`·`○○항`처럼 2자+ 한정. 취향 칩은 **8종 다중선택 AND**(`currentThemeFilters`+`matchesTheme`) 고정(2026-07-03 P1).
- ❌ **지역 필터를 `location.includes(칩이름)`로 직접 매칭 금지.** 데이터는 약칭(충북/충남/경남/부산/전북…)이라 "충청"=0건이 됨(엑셀 오류 아님). 반드시 `REGION_MATCH`(광역권→약칭들) + `matchesRegion()` 경유 → 전 코스 도달 보장.
- ❌ 파괴적 git(reset --hard·force push) 사용자 승인 없이 금지. 기본 브랜치 직접 커밋·푸시 금지(브랜치 사용).
- ❌ **프로필(아바타)+필명은 오직 상단 우측 앱바(`appbar-profile`)에만 노출.** 본문에 프로필 스트립 중복 금지(만보기·헬스 본문 프로필행 금지). **등급줄("초보 걷기꾼 · 후기 N개" 등) 절대 노출 금지** — 등급/후기수 표기 자체를 화면에 띄우지 않는다. (2026-06-29 재지적, §0)
- ❌ **국내 폭포 상세 사진에 다른 폭포·범용 스톡·키워드 랜덤 사진을 실제 사진처럼 노출 금지.** 폭포명+지역(또는 좌표·고유 형상)+재사용 권리를 확인한 실제 사진만 `실제 사진`으로 표시한다. 정확한 재사용 사진을 확보하지 못한 곳은 공식 설명·지형·계절 근거를 바탕으로 포토리얼 이미지를 생성할 수 있으나, 화면과 원장에 반드시 **`AI 생성 재현 이미지 · 실제 촬영 사진 아님`**이라고 표시한다. 실제/AI 구분, 근거 URL, 저작자·라이선스, 생성 프롬프트, 파일 해시가 없는 이미지는 배포 금지. (2026-07-19 확정)

## 5. 작업 후 검증 (필수)
- `node --check app.js` + Claude Preview(`.claude/launch.json`의 `gonga`, 375px)로 **실제 화면 확인**. preview serverId는 **잘리지 않은 전체 문자열** 사용(부분 id는 eval만 맞고 screenshot은 stale).
- 콘솔 에러 0 확인. 스크린샷이 stale이면 stop→start 재기동 또는 eval로 DOM 직접 검증.

## 6. 스킬·도구 라우팅 (작업 → 도구, 재질문·우회 금지)
| 작업 | 스킬/도구 (반드시 사용) |
|---|---|
| **UI/UX 디자인·레이아웃·스타일·개선·리뷰** | **`ui-ux-pro-max` 스킬 호출**(수동 추정 금지). 디자인시스템/팔레트/UX 가이드 조회 후 적용 |
| 화면 미리보기·검증 | Claude Preview MCP (`preview_start` gonga → resize 375 → screenshot/eval) |
| 코스 이미지 | **실사만**(키워드 매칭 loremflickr). 일러스트·이모지 금지 |
| 추천 코스 | 현재 계절(`getCurrentSeason`) + 날씨(`weatherInfo`) 연동 필수 |
| 새 폴더 하네스 | `/harness-setup` |
| 지식그래프 | `/graphify` |

## 7. 워크플로우 라우팅
상세는 `docs/WORKFLOWS.md`.

## 8. 마감 루틴 (Closeout) — 작업 완료 = 자동 마감 (머지·배포까지, 매번 확인 없이)
> **[2026-07-04 개정, §0] 지시받은 작업이 검증까지 끝나면 별도 "마감처리" 언급이 없어도 아래 마감 루틴을 머지·배포까지 자동 실행한다.** (사용자 재지시 2회 발생 — "마감처리까지 해라고 않했나" — 커밋 보류하고 기다리는 것 금지.) "마감처리 / 마감 / closeout" 단어는 수동 트리거로도 계속 유효.
1. **지침·작업방법 기록**: 이번 세션의 지시·교정·선호·결정을 §4(금지)·§6(라우팅)·§9(Decision Log)·`docs/CONVENTIONS.md`·전역 `memory/`에 반영(재설명 방지, §0).
2. **검증**: `docs/VERIFY.md` 실행(`node --check` + Claude Preview 375px + 기능 체크리스트), 콘솔 에러 0 확인.
3. **md 일치 점검**: AGENTS/CLAUDE/GEMINI/docs에 `<placeholder>` 잔존 0, §9에 이번 작업이 기록됐는지 확인.
4. **커밋**: 작업 브랜치에 의미단위 커밋(메시지 끝 `Co-Authored-By: Claude ...`). ❌ 기본 브랜치 직접 커밋 금지.
5. **푸시 + PR**: 브랜치 push → master PR 생성/갱신(`gh pr create` / `gh pr edit`). ❌ 기본 브랜치 직접 푸시 금지.
   - **기계 강제(2026-08-16 신설)**: `.githooks/pre-push` 가 `master` 로의 푸시를 exit 1 로 막는다. **클론마다 1회** `git config core.hooksPath .githooks` 를 실행해야 걸린다(git 은 훅을 버전관리로 배포하지 않는다). 의도적 우회는 `--no-verify` 이고 근거를 남긴다.
   - 신설 이유: 이 조문이 문서에만 있어 **2026-08-16 실제로 어겼다**(`132352f` 를 master 로 직접 푸시). 규칙만 있고 채점하는 것이 없으면 반복된다 — 같은 날 ZEROS `prod-probe` 가 `--strict` 없이 FAIL 을 exit 0 으로 흘리던 것, CanaryLab 게이트가 빌드 앞에서 옛 배포본을 재던 것과 같은 부류다.
6. **배포(해당 시)**: 정적 웹앱이면 GitHub Pages 빌드 트리거(`gh api -X POST repos/OWNER/REPO/pages/builds`) → 라이브 URL을 curl로 검증(title/마커). HTTPS 필수. (현 배포: https://ihaelyong988-lab.github.io/gongacourse/)
7. **정리**: 임시 미리보기 서버 종료 등 뒷정리.
8. **순번 결과 보고**: 한 일을 ①②③… **순번으로 간결히** + 라이브 URL·PR 링크·남은 1클릭(머지)을 명시.
※ master 직접 푸시·배포는 런타임 보안 가드가 막을 수 있음 → 항상 **PR/Pages 경유**로 처리하고, 막히는 그 1단계만 사용자에게 고지(나머지는 자동 진행).

## 9. 작업 이력 · 지시 맥락 (Decision Log) — append-only
### 2026-06-28 — 모바일 네이티브 전면 재구축 (v2.0)
### 2026-08-23 — 검색엔진 등록 준비: head 메타 3종 신설

> 주인님 지시: zeros·미친가치·천책빵·꽁아코스를 네이버·구글에 등록. 등록 규정·절차 검토 후 필수 2건만 착수.

1. **변경**: `index.html` head 에 `description` · `canonical` · `og:type/site_name/title/description/url`. 라이브 실측에서 전부 0건이었다 — 검색결과에 제목 한 줄만 나가고 메신저 미리보기가 비었다.
2. **판정 근거**: 이 앱은 `screenHash()` 해시 라우팅이라 **색인 가능한 고유 URL 이 1개**다. 그래서 sitemap 은 실익이 없고(크롤러가 홈에서 바로 본다) head 메타가 검색 노출 품질의 전부다. 코스별 정적 HTML 생성은 라우팅을 경로 기반으로 바꾸는 아키텍처 변경이라 등록 작업과 분리했다.
3. **미결**: `og:image` 없음(대표 이미지 파일 미보유) · 코스 카드 썸네일이 `coursePhoto()` 기본값으로 `loremflickr` 랜덤 스톡을 쓴다 — §4 는 폭포만 막았고 코스 카드는 구멍이다. 공개 노출 품질·라이선스 문제라 별건으로 남긴다.
4. **네이버 등록 선행 확인**: 이 사이트는 `github.io/gongacourse/` 하위 경로다. 네이버가 하위 경로 등록을 받는지 미확정(공식 가이드가 봇에 HTTP 500) — 받지 않으면 `ihaelyong988-lab.github.io` 루트 저장소에 소유확인 파일이 필요하다(현재 루트 404).

> 사용자 불만: 뒤로가기로 정보 못 찾음 / 코스 그래픽 허접·화면 벗어남 / 맞춤설정 뭘 맞추는지 모름 / 코스탐색 필터 작동 안 함 / 홈 작동 안 함 / 소통 채널 컨셉 부재. 앱 정체성 = "경험자와 소통하는 가이드북".
1. **변경**: 데스크톱 2단 대시보드 → 화면 스택 + 하단 5탭(홈/탐색/소통/저장/내정보)로 전면 재구축. ①뒤로가기=화면 복귀(history 라우터, 종료 confirm은 홈에서만) ②가짜 SVG 지도 → 코스별 실제 세로 타임라인 + 카카오맵 딥링크 ③맞춤설정=결과 즉시 시각화("효도코스 N개 우선") + 내정보로 흡수, 개발자 엑셀도구 제거 ④`<select>` → 칩 필터(지역/테마/계절) ⑤추천 홈 신설(오늘의 추천1+상황별 바로가기+맞춤배너) ⑥소통 탭 신설(전체 후기 피드+인증샷 갤러리).
2. **원인/교훈**: 근본원인은 "PC 대시보드를 폰에 욱여넣음". 데이터(242코스)는 그대로 두고 화면구조·내비·시각화만 폰 네이티브로 교체. 결정사항: 홈 = 오늘추천1+상황바로가기+맞춤배너(후기피드는 소통탭). 식당 전화는 가짜(010-1234)라 제거 → 카카오맵 링크로 대체.
3. **검증**: node --check 통과, preview 375px에서 6화면+뒤로가기+칩필터+맞춤배너 전부 정상, 콘솔 에러 0. 브랜치 `redesign/mobile-native` (커밋 보류 — 사용자 승인 시).

### 2026-06-28 — 홈 첫화면 세로 공간 압축
> 사용자 지시: "첫화면을 작업전 형식으로(=원래 리스트처럼 빽빽하게) 세로폭 공간 효율화". 선호 규칙: **홈은 한 화면에 최대한 많은 정보, 큰 히어로·큰 버튼으로 세로 낭비 금지**.
1. **변경**: 오늘의 추천 카드 압축(큰 "코스 자세히 보기" CTA 제거→카드 전체 탭+우상단 화살표, loc·시간·만족·맛집을 메타 1줄로 통합) · 맞춤배너→슬림 1줄 바(`match-bar`) · 상황별 바로가기 2줄 그리드(`sit-grid`)→가로 스크롤 1줄(`sit-scroll`/`sit-chip`) · 섹션라벨 여백 축소(`home-label`, 더보기 링크 추가) · 후기 2→3개. 결과: 오늘추천+맞춤바+상황+후기3 전부 한 화면.
2. **교훈**: 폰 홈은 "빽빽한 정보 밀도"가 선호. 가로 스크롤로 행 수를 줄여 세로 절약. 이 밀도 선호를 향후 다른 화면에도 적용.
3. **검증**: preview 375px 스크롤 없이 4섹션 노출 확인, 카드 클릭→상세 정상, 콘솔 에러 0. (주의: preview 렌더러가 멈추면 stop→start로 재기동.)

### 2026-06-28 — 오늘의 추천 계절·날씨 연동
> 사용자 지시: "추천 코스를 현재 계절·날씨와 연동. 한여름에 겨울 코스 뜨면 앱이 왜 필요한가." → §4 규칙으로 승격.
1. **변경**: `pickTodayCourse()`를 `getCurrentSeason()`(월→계절) 풀로 강제. 실시간 날씨(`loadWeather`, Open-Meteo + geolocation, 키 불필요)로 보정 — 비/눈=짧고 쉬운 코스, 무더위=숲·계곡, 추위=짧은 코스. 홈 라벨에 "🌿 여름 · 32° ☀️" 컨텍스트 칩 + 보정 시 안내 문구. 날씨 1h 캐시(localStorage `gongacourse_weather`).
2. **교훈**: 위치 거부/오프라인이면 계절-only로 자동 폴백(필수). 날씨는 비동기 → 로드 후 홈 재렌더.
3. **검증**: 6월=여름 코스 노출 확인(겨울코스 사라짐), 위치거부 폴백·날씨주입(hot) 양쪽 정상, 콘솔 에러 0.

### 2026-06-28 — 카드 가로형 미디어 레이아웃 + 계절 CSS 비주얼
> 사용자 지시: "도형+내부 이미지+설명문구 재배치로 세로 효율 개선(전문가 기법)". 확정: 대상=홈+코스카드, 이미지=CSS 생성(오프라인 안전).
1. **변경**: ①`.season-visual`+`.sv-{season}` — 계절별 그라데이션 + 산 실루엣(인라인 SVG data URI) + 태양 + 이모지로 **사진 없이 도형 내부 비주얼** 생성(외부 의존 0). ②`courseCardHtml` 세로 스택 → **가로형 미디어 카드**(비주얼 74px 좌 · 문구 우, `cc-visual`/`cc-main`). ③홈 '오늘의 추천' → 비주얼 104px 좌 + "추천" 리본 + 문구 우(`tc-row`/`tc-visual`/`tc-content`).
2. **교훈**: 세로 효율의 정석 = **세로 스택을 좌우 미디어 카드로 묶기**(높이↓, 한 화면 밀도↑) + 사진 없으면 CSS 그라데이션/SVG 실루엣으로 비주얼 대체(오프라인 §4 준수). 텍스트는 ellipsis로 1줄 클램프. 이 미디어카드 패턴을 향후 신규 카드 기본형으로.
3. **검증**: preview 375px 홈 여름비주얼·탐색 겨울비주얼 카드 정상, 가로 오버플로우 0, 콘솔 에러 0.

### 2026-06-28 — 실사 사진 전환 + 탐색 검색/필터 재배치 (일러스트 폐기)
> 사용자 강한 피드백: "그림(일러스트) 넣으면 장난하는 줄 안다 → **실사**여야 실감난다." + 탐색=맞춤검색창이니 "검색창을 최상단 로고 옆으로, 지역·테마·계절 필터를 한 줄로, 리소스(결과) 최대화".
1. **변경**: ①`season-visual` 일러스트(이모지+산 SVG) **폐기** → `coursePhoto()` 키워드 매칭 **실사**(loremflickr, 계절/사찰/바다 키워드, `lock=id`로 고정). `<img onerror>` 실패 시 `sv-{season}` 그라데이션 폴백. ②검색창을 앱바(로고 옆 최상단)로 이동(`onGlobalSearch`, 입력 시 탐색으로 라우팅) — 탐색 내 검색줄 제거로 세로 절약. ③탐색 필터: 3줄 라벨형 → **한 줄 가로 스크롤 필터 바**(`filter-bar`, 지역|테마|계절 구분), sticky. ④카드 비주얼 84px 실사.
2. **교훈(영구 선호)**: **코스 이미지는 반드시 실사**(일러스트·이모지 금지). 외부 이미지는 키워드 매칭 + onerror 폴백 필수. 탐색=검색 우선·필터 1줄·결과 최대화. ※초기에 CSS 일러스트를 권장했다가 반려됨 — 다음부터 이미지는 실사 우선 제안.
3. **검증**: preview 375px 실사 사진(산·숲·사찰) 정상, 검색창 로고 옆, 한 줄 필터(서울 7개) 정상, 콘솔 에러 0. ※loremflickr 지연/불안정 가능 → 폴백 유지, 추후 정식 이미지 호스팅 검토.

### 2026-06-28 — 탐색 필터 = 3탭 + 수납장(서랍) 패턴 [확정 스펙·변경 금지]
> 사용자 재지시(불만): "한 줄로 나열"의 의미 = 모든 칩을 한 줄에 펼치는 게 **아니라**, **지역·테마·계절 3개 탭을 한 줄에 두고 → 각 탭을 누르면 수납장(서랍)처럼 세부 항목이 펼쳐지는** 아코디언. 같은 설명 반복하게 만든 것 = §0 위반.
1. **변경(확정 UI)**: `renderExploreFilters()` — `ftab-row`에 지역/테마/계절 3개 `.ftab`(각 "라벨+현재값+▾"). 탭 클릭=`toggleDrawer(group)` → 그 그룹의 `.filter-drawer`(칩 묶음)만 아래로 펼침(아코디언, 동시에 1개만). 항목 선택=`setRegion/Theme/Season` → 서랍 닫고 `renderExploreFilters()`+`renderExploreList()`. 값 선택 시 탭은 `has`(초록) + 우측 `ftab-clear`(↺) 노출. **서랍 토글은 리스트 재렌더 안 함**(이미지 깜빡임 방지).
2. **교훈(영구)**: 필터 UI는 **3탭+서랍(수납장)** 고정 — 모든 칩 한 줄 노출 방식으로 되돌리지 말 것. 사용자가 "수납장/서랍/펼침"을 말하면 아코디언 disclosure를 뜻함. **새 작업 전 본 §9·§4를 먼저 읽어 같은 지시 반복 금지(§0).**
3. **검증**: 지역 탭→서랍 펼침→서울 선택→탭 "지역 서울"·7개 필터·초기화 노출, 콘솔 에러 0.

### 2026-06-28 — 소통 탭: 상단 업로드 컴포저 + 하단 후기 필터 탭
> 사용자 지시: 소통을 사용자 관점으로 — "상단에 사진/카메라로 쉽게 업로드, 하단 방문후기는 최신순·평점순·검색 탭으로 필터링".
1. **변경**: ①**상단 컴포저**(`composer`): 텍스트 + 코스명(선택) + 카메라 버튼(`capture=environment`) + 올리기. 자유 게시물은 코스에 묶이지 않고 별도 저장소 `communityPosts`(localStorage `gongacourse_posts`)에 저장. ②**하단 피드**: `communityFeed()`가 자유게시물 + 전체 코스 후기를 통합 정규화. **정렬 탭** `최신순|평점순|검색`(`commSort`) — 평점순=후기 ratings 평균 내림차순, 검색=검색창 노출 후 텍스트/작성자/코스명 필터. ③피드 카드에 별점·사진·코스태그 표시, 본인 자유글은 삭제 가능. 게시 후 `commSort='latest'`로 리셋해 새 글 상단 노출.
2. **교훈**: 데이터가 코스 종속이라 자유 업로드는 별도 `communityPosts` 저장소로 분리(코스 후기와 피드에서 통합 표시). 정렬/검색은 리스트만 재렌더(이미지 깜빡임 방지). 카메라는 `<input capture>`로 모바일 직촬 지원.
3. **검증**: preview 375px 컴포저 게시(피드 최상단+영속), 최신/평점(4.7 우선)/검색 탭 전환·필터 정상, 콘솔 에러 0. ※preview는 반드시 현재 serverId 사용(옛 id는 stale 페이지).

### 2026-06-28 — 소통 정정: "검색순"=인기검색 정렬 + 컴포저 간소화 [용어 확정]
> 사용자 정정(중요): **"검색순" = 검색창이 아니라 "많이 검색된 키워드(인기) 순 정렬"**. 컴포저는 "사진/갤러리 + 간단의견 + 올리기"로 간소화·직관화(코스명 등 군더더기 제거).
1. **변경**: ①정렬 탭 `검색`(검색창)→**`검색순`(popular)**: 앱바 검색 시 `searchCounts`(localStorage `gongacourse_searchcounts`) 누적(`bumpSearch`, 900ms 디바운스) → 검색순은 `itemPopularity`(코스 votesUp+댓글*3+만족도 + 검색어매칭*20)로 정렬. 상단에 **인기 키워드 칩**(`topKeywords`, `#키워드 횟수`) 노출, 칩 탭=그 키워드 후기만(`commKeyword`). 빈 상태=힌트. ②컴포저 간소화: 코스명 입력 제거, **간단의견 1줄 + [사진(카메라 capture)]·[갤러리(capture 해제)]·[올리기]** 3버튼 균등 배치(`composer-link`/`composer-post`). ③소통 검색창(`comm-search`)·`onCommSearch`·`commSearch` 전면 제거.
2. **교훈(용어 확정)**: 사용자 어휘 **"검색순"=인기/트렌딩 정렬**(검색박스 아님). "탭링크"=눈에 띄는 버튼. "간소화/직관적"=필드 최소화+큰 버튼. preview **serverId는 잘리지 않은 전체 문자열** 사용(부분 id는 eval은 fuzzy매치되나 screenshot은 stale 프레임 → 오판 주의).
3. **검증**: 검색어 주입(북한산5·단풍3)→칩 `#북한산 5 #단풍 3`, 북한산 코스 후기 인기도 최상단, 칩필터 2건 정확, 콘솔 에러 0(eval 검증).

### 2026-06-28 — "사각도형나라" 탈출: 수평 라인 밴딩 + 박스 약화 (ui-ux-pro-max 스킬 적용)
> 사용자 지시: 화면이 사각 박스만 쌓여 단조로움 → "수평라인으로 살짝 포인트, 안정감 있게 박스나라 탈출". + UI/UX 스킬 썼냐는 지적. → 이번부터 `ui-ux-pro-max` 스킬 실제 호출해 적용(이전엔 수동 원칙만).
1. **변경**: ①`sectionLabel()` 헬퍼 — 섹션 제목 + **수평 hairline(그라데이션)** + 우측요소. 홈 3개 섹션·소통 "방문후기"에 적용. ②피드를 **박스→hairline 행**(`.feed-item`/`.feed-card` border 제거, `border-bottom: var(--hair)`)으로 = 박스나라 탈출 핵심. ③카드 제목(`d-card-title`/`set-title`)에 **하단 hairline** 밴딩. ④카드 테두리 `--border`→**`--hair`(연하게)**로 약화(course/today/d-card/set-card/composer). ⑤anti-pattern(AI 보라/핑크) 회피 → 맞춤배너 **보라→그린**. 토큰 추가 `--hair #edf0ed`, `--accent-line #cfe0d2`.
2. **교훈(영구 디자인 원칙)**: 박스 단조로움 탈출 = **세로 스택 박스 줄이고 수평 hairline으로 밴딩**(섹션 헤더 라인 + 리스트는 박스 대신 구분선 행). 카드 테두리는 `--hair`로 은은하게. **UI/UX 작업은 `ui-ux-pro-max` 스킬을 호출해 적용**(수동 추정 금지). 보라/핑크 그라데이션 회피, 그린 자연 팔레트 유지.
3. **검증**: preview 375px 홈(섹션 라인+그린 배너+hairline 후기)·소통(방문후기 라인+hairline 피드)·상세(카드 제목 라인) 정상, 콘솔 에러 0.

### 2026-06-28 — 하네스 규칙 강제주입 훅 + 내정보=만보기 계기판
> ① 사용자 분노: md에 적어도 우회됨 → 원인=UserPromptSubmit 훅이 고정문구만 주입(실제 규칙 X). ② 내정보에서 효도코스(맞춤설정)·오프라인·앱정보 삭제, 만보기 계기판+기록 신설.
1. **변경**: ①`scripts/harness-context.mjs` — AGENTS.md §4/§6/§9를 매 턴 컨텍스트 강제주입(우회 차단). settings.json 배선은 **사용자 승인 대기**(자가수정 가드 차단). ②mypage: 동반자/교통 카드·오프라인·앱정보 제거 → **만보기 대시보드**(SVG 원형 게이지 `ped-gauge` + km/kcal/목표 + 목표칩 + 걸음기록 input·+칩 + 7일 막대). 저장소 `gongacourse_steps {goal,days}`. ③동반자 개인화는 **홈 배너로 이동**(`match-card` 칩 `setCompanionHome`)해 끊김 방지. 화면 접근성은 mypage 유지.
2. **교훈**: 규칙은 기록만으론 강제력 0 → **훅이 실제 규칙을 주입해야** 적용됨. 만보기는 센서 없이 기록형(localStorage). 설정 삭제 시 의존 기능(홈 배너) 이전 필수.
3. **검증**: 6,420보→4.49km·257kcal·게이지80%(offset65.3), 주간바7·오늘80%, 삭제3종 제거 확인, 홈 동반자칩→효도코스 결과, 콘솔 에러 0(eval).

### 2026-06-28 — 만보기 실센서 측정(DeviceMotion) + 내정보 콤팩트 대시보드
> 사용자: 내정보 상단 박스 콤팩트 정돈, 만보기를 대시보드로 한 화면에, "폰에서 켜고 움직이면 카운트되는지" 확인.
1. **변경**: ①프로필 가로 1줄(`profile-row`/`avatar-sm`)·스탯 압축. ②만보기 대시보드화: 게이지(132px)+측정버튼+km/kcal+목표칩을 `ped-main` 2단 배치, 주간바·빠른칩 압축 → 한 화면. ③**실시간 걸음 측정**: `DeviceMotion`(가속도계) 피크검출(`onDeviceMotion`, 저주파분리 dyn>1.2, 간격 280ms, iOS `requestPermission`). `countOneStep`→`updateGaugeLive`(전체 재렌더 없이 게이지/숫자 즉시 갱신, 10보마다 저장). `측정 시작/정지` 토글.
2. **교훈**: 웹 만보기는 DeviceMotion으로 실제 카운트 가능하나 **실기기+HTTPS+동작권한** 필수(PC 프리뷰는 센서 없어 미작동 → eval로 합성이벤트 검증). 라이브 카운트는 DOM 직접 갱신(재렌더 금지).
3. **검증(eval 합성)**: 걷기 12회→12보 카운트+게이지 라이브갱신, 노이즈 0, 간격가드 중복 0, 콘솔 에러 0.

### 2026-06-28 — 만보기 계기판 고도화: 모던 270° 게이지 (ui-ux-pro-max 적용)
> 사용자: 만보기 계기판을 최신·고도화, 단순하면서도 직관적으로.
1. **변경**: ui-ux 가이드(Soft UI Evolution·Gauge=gradient+값/%label+SVG) 적용 → 단순 원형 링 → **270° 하단개방 계기판**: `pathLength=100` 정규화, `g-track`(75)/`g-prog`(pct*0.75) + **그라데이션 stroke**(`#pedGrad` 연→진녹) + **팁 도트**(`g-tip`, 진행끝 흰점) + 중앙 풋스텝아이콘·큰 숫자·% + 하단 개방부 **상태 알약**(목표까지 N보 / 🎉 목표 달성=호박색). 0.55s ease 애니메이션. `updateGaugeLive`도 dasharray+팁좌표+상태 갱신으로 교체.
2. **교훈**: 게이지는 `pathLength=100`으로 정규화하면 각도 계산 단순(75=270°). 라이브는 dasharray/cx/cy 직접 갱신(재렌더 X). 달성 시 색 전환(녹→호박)으로 직관성↑.
3. **검증**: 70%→그라데이션 호+팁도트+"목표까지 2,400보", 100%→호박 풀게이지+"🎉 목표 달성"+주간바 호박, 라이브 8,200→8,205 갱신, 콘솔 에러 0.

### 2026-06-28 — 내정보 컬러 포인트 + 프로필 사진 등록 + 스탯 슬림 가로배치
> 사용자: "초록나라" 완화(거슬리지 않는 컬러 포인트), 프로필 아이콘 사진 등록, 저장/후기/전체 박스 세로 축소+문자 가로배치(중요 정보 아님).
1. **변경**: ①ui-ux 팔레트(그린+오션블루+솔라골드) → 스탯 아이콘에 **은은한 포인트**(저장=teal, 후기=amber, 코스=blue). ②프로필 아바타=버튼+카메라 배지 → 탭 시 사진 등록(`triggerAvatar`/`handleAvatar`, 중앙 정사각 크롭 200px, `visitorSettings.avatar` 저장, 원형 표시). ③`profile-stats`(3박스 세로) → **`mini-stats` 슬림 1줄 가로**(아이콘+숫자+라벨 inline, hairline 구분, 세로 ~70→44px).
2. **교훈**: "초록나라" 완화는 보조색 남발이 아니라 **의미있는 1~2색을 아이콘에만 은은히**(green+blue+amber 조화). 비중요 정보는 슬림 가로 스트립으로.
3. **검증**: 슬림 스탯 가로배치·3색 아이콘 노출, 아바타 사진주입→원형표시+배지, 콘솔 에러 0.

### 2026-06-28 — 만보기 "목표까지 N보" 상태칩을 제목 줄로 이동
> 사용자: 상태칩이 게이지 중앙 카운트와 겹쳐 갑갑 → "만보기" 제목과 같은 수평 라인(우측)으로.
1. **변경**: `gauge-status`(게이지 하단 절대배치) 제거 → `ped-title`(set-title flex space-between) 우측에 `status-pill` 배치. `gauge-wrap` 높이 178→162 + overflow hidden으로 빈 하단 트림. `updateGaugeLive`는 id `ped-status`라 위치 무관 그대로 동작.
2. **교훈**: 게이지 중앙(카운트)은 숫자만 — 부가정보(목표 잔여)는 제목 줄 여백으로. 절대배치 오버랩 주의.
3. **검증**: 제목 우측 "목표까지 4,800보", 라이브 8,500→"🎉 목표 달성", 2,000→"목표까지 6,000보", 콘솔 에러 0.

### 2026-06-28 — 내정보 프로필을 앱바 우측으로 이동(아이콘 최우측)
> 사용자: 프로필 아이콘+이름을 최상단 "꽁아코스" 행으로, 아이콘 최우측.
1. **변경**: 본문 `profile-row` 제거 → 앱바에 `appbar-profile`(이름+등급 2줄 + 아바타 최우측, `margin-left:auto`). `renderAppbarProfile()`가 채우고 `showScreen`이 mypage에서만 노출(타 화면 hidden). 아바타 업로드(`triggerAvatar`/`handleAvatar`)는 앱바로 이동, `handleAvatar`→`renderAppbarProfile()` 갱신.
2. **교훈**: 화면별 앱바 컨텐츠는 `showScreen`에서 토글(검색=홈·탐색, 프로필=내정보). 본문 헤더를 앱바로 올리면 세로 절약.
3. **검증**: 앱바 우측 이름+등급+아이콘 노출, 아바타 주입 반영, 홈 hidden·내정보 flex, 콘솔 에러 0.

### 2026-06-28 — "마감처리" 자동 절차 확정 (§8 전면개정 + 훅 §8 주입)
> 사용자: "마감처리"라 하면 매번 확인 없이 지침·작업방법 기록→커밋→푸시·PR→배포→순번 보고까지 전부 자동.
1. **변경**: §8을 8단계 확정 체크리스트로 개정(기록→검증→md점검→커밋→푸시+PR→배포→정리→**순번 결과보고**). `harness-context.mjs`가 §8까지 매 턴 주입하도록 수정(누락 방지). 동일 적용: **꽁아코스 + 하네스 킷(settings.json도 규칙주입 훅으로 교체) + Hermes_Workspace**.
2. **교훈**: 절차도 훅으로 주입해야 트리거가 안 샌다. master 직접푸시·배포는 가드가 막을 수 있으니 PR/Pages 경유 고정.
3. **검증**: 3곳 훅 모두 "마감처리" 텍스트 주입 확인, 킷 settings.json 유효.

### 2026-06-28 — 마감·배포: PR + GitHub Pages(HTTPS)
> 사용자: 마감처리 후 배포.
1. **변경**: `redesign/mobile-native` 푸시 + **PR #1**(master←branch) 생성. master 직접 푸시는 §4·보안가드로 차단 → PR로 마감. **GitHub Pages 소스를 브랜치로 전환해 v2.0 배포**(빌드 트리거 필요: `gh api -X POST .../pages/builds`).
2. **교훈**: 기본 브랜치 직접 푸시는 막힘(정상) → PR 경유. Pages 소스 변경만으론 재빌드 안 됨 → 빌드 명시 요청. 배포 URL: https://ihaelyong988-lab.github.io/gongacourse/ (HTTPS=실기기 만보기/날씨 작동). PR 머지 후 Pages를 master로 되돌리면 됨.
3. **검증**: 라이브 title "경험자와 소통하는 나들이 가이드"·"만보기" 탭·app.js v2.0 마커 확인.

### 2026-06-28 — 탭 "내정보"→"만보기" + 만보기 히어로 풀링 계기판 재설계
> 사용자: 하단 탭을 "만보기"로, 계기판이 겹치고·균형/위계 없고 조잡 → 직관 재설계.
1. **변경**: ①하단 탭 `mypage` 라벨 "만보기"+`fa-shoe-prints`. ②270° 게이지(overflow 클립으로 겹침) 폐기 → **풀 링 게이지**(`r-track`/`r-prog` dashoffset, rotate(-90) 12시 시작, 팁 도트, 그라데이션). **위계**: 걸음 수 40px(유일 포인트)·km/kcal는 박스→inline 보조·목표/주간/수동은 **별도 "걸음 기록" 카드로 분리**. 히어로 카드(soft shadow)로 분리. `updateGaugeLive`도 dashoffset로 교체.
2. **교훈**: 계기판 겹침 원인=270° + overflow 클립 → 풀 링이 균형·무클립. 위계=핵심 1개만 크게, 나머지 inline/별도카드로 강등. ui-ux 타입스케일(40/14/12/11) 적용.
3. **검증**: 80%→그라데이션 풀링+팁+"목표까지 1,580보", 100%→호박 풀링+"🎉 목표 달성"+주간 호박, 라이브 8,300→8,304, 탭 "만보기", 콘솔 에러 0.

### 2026-06-28 — 만보기 화면꺼짐 방지(Screen Wake Lock) — "끄면 안 되던" 문제 부분해결
> 사용자: 만보기가 화면 꺼지면 카운트 끊기는 문제 — "당신 능력으로 가능해, 해결". 웹 한계 안에서 최대 해결 요구.
1. **변경**: `navigator.wakeLock` 도입(`requestWakeLock`/`releaseWakeLock`). `startStepTracking`에서 화면 잠금 획득 → 측정 중 **화면 자동꺼짐 방지**(센서 이벤트 유지) → 끊김 없이 카운트. `stopStepTracking`에서 해제(배터리). `visibilitychange` 복귀 시 `motionActive`면 재획득(앱 전환 시 자동 해제 대응). 미지원·거부는 graceful 무시.
2. **교훈(영구·정직한 한계)**: 웹 만보기 백그라운드 미작동의 **실해결책 = Wake Lock으로 화면을 안 꺼지게** 잡는 것(센서가 멈추는 근본원인=화면 꺼짐). ✅ 해결: 화면 자동잠금으로 끊기던 것. ⚠️ 불가(원리적): 전원버튼 강제 OFF·완전 백그라운드 = 네이티브 앱만 가능 → 사용자에게 항상 정직 고지(CONVENTIONS §정직 고지). Wake Lock은 **secure context(HTTPS/localhost)** 필수 → 실기기 라이브에서만 확인.
3. **검증**: `node --check` 통과. 실동작은 실기기+HTTPS 필요(PC 프리뷰 센서 없음).

### 2026-06-28 — 헬스탭 신설 + 이미지 컨텍스트 매칭 + 폰트 1단계 + 사찰필터 버그수정 (ui-ux-pro-max 적용)
> 사용자: ①만보기 가독성 리디자인+케이스 닫아도 작동 확인 ②하단 '저장'탭→'헬스'(만보기+트레킹+예방건강 창의 구성) ③추천 이미지를 사유 핵심·계절·맥락에 맞게(삼형제섬에 법당 사진 금지) ④상세에 사전 준비물·검토 변수 키워드 ⑤전체 폰트 한 단계↑ ⑥전수 사용자테스트 고도화.
1. **변경**: ①**만보기**: 측정 중 `.measure-banner`("● 측정 중 · 🔆 화면 꺼짐 방지 ON") 표시로 Wake Lock 작동 가시화 + 안내문 정직화(케이스/전원 강제OFF는 웹 한계). ②**헬스탭**(구 저장, `data-tab=saved` 유지·라벨/아이콘 `헬스`/`fa-heart-pulse`): `renderSaved`를 건강 허브로 재작성 — 건강 히어로(걸음/km/kcal/활동일+진행바)·트레킹 지표(저장코스→예상 km·kcal 추정)·예방건강 미션(`healthMissions` 걸음 연동)·계절/날씨 팁(`healthTip`)·저장코스 리스트 보존. ③**이미지**: `photoKeyForCourse(c)` 신설 — 제목 지형(섬/바다/호수/계곡/강/숲/공원/산) 우선, temple은 제목이 사찰일 때만 → 삼형제섬=sea. PHOTO_KW에 lake/valley/forest/park/mountain/river 추가. ④**상세**: `coursePrep`/`courseChecks` → "출발 전 체크" 카드(준비물=green칩·검토변수=amber칩, 계절·난이도·지형 맥락 자동). ⑤**폰트**: `:root --fs 1→1.1`, font-large 1.15→1.27·xlarge 1.3→1.45(일괄 상향). ⑥**버그수정(전수QA)**: `hasTemple`의 단독 '사' 매칭이 '식사'를 잡아 사찰필터 224/242 폭주 → `spotIsTemple()`(사찰전용어+'○○사/암'토큰, 비사찰어 제외)로 교체 → 86개 정상화. `stepIcon` ⛩️도 동일 적용.
2. **교훈(영구)**: 이미지=코스 정체성(제목 지형) 우선·사찰은 제목일 때만(§4). 한글 사찰 판별에 단독 '사/절/암' 금지(식사·계절 충돌). 만보기 Wake Lock은 **작동을 UI로 가시화**해 사용자 안심. 폰트 일괄 상향은 `--fs` 기준값만 올리면 전 요소(calc) 반영.
3. **검증**: preview 375px — 헬스 히어로/트레킹/미션 정상, 만보기 측정배너, 상세 준비물 13칩(삼형제섬→승선권·여객선물때), 홈 추천=해변사진, 사찰필터 224→86, 폰트 16.5px, 콘솔 에러 0.

### 2026-06-28 — 지역 필터 "충청" 0건 버그수정 (약칭 매핑)
> 사용자: "충청 검색에 왜 하나도 없나? 엑셀 등록 오류인가?"
1. **원인**: 엑셀/데이터 오류 아님 — `location`은 정상으로 `충북`(36)·`충남`(18)·`세종`(2)로 저장. 버그는 **필터**: 칩 이름 `"충청"`을 `location.includes("충청")`로 매칭 → 약칭이라 0건. 같은 이유로 `경남`(25)·`전북`(20)·`부산`(1)은 칩 자체가 없어 도달 불가였음.
2. **변경**: `REGION_MATCH`(광역권→약칭 배열: 충청=충북/충남/대전/세종, 경북=경북/대구, 경남=경남/부산/울산, 전라=전북/전남/광주) + `matchesRegion(c,region)` 신설. `REGIONS=Object.keys(REGION_MATCH)`(경남·전라 칩 추가). `getFilteredCourses` 지역검사를 `matchesRegion` 경유로 교체.
3. **검증**: 충청 0→56, 경남 28·전라 42 칩 신설, **도달 불가 코스 0/242**, 지역 서랍 10칩 노출, 콘솔 에러 0.

### 2026-06-28 — 만보기 자동재개 + 자정 자동리셋 (+ 플립 한계 정직 고지)
> 사용자: "플립 닫으면 측정 안 됨. 앱 사용 여부와 무관하게 늘 일간 단위로 리셋·측정되게."
1. **원인/한계(정직)**: 플립 닫힘=화면 꺼짐 → 브라우저가 DeviceMotion 정지. **모든 웹앱 공통 제약, 우회 불가**(진짜 백그라운드 만보기는 네이티브만). Wake Lock도 케이스/전원 강제 OFF는 못 막음.
2. **변경(가능한 최대)**: ①**측정 ON 의도 영속화** `stepData.tracking` → `startStepTracking`서 true 저장, `stopStepTracking`서 false. ②**자동 재개** `resumeTrackingIfWanted()` — 콜드 로드(DOMContentLoaded)·탭 복귀(visibilitychange)서 tracking이면 자동 `startStepTracking`(매번 시작버튼 불필요). Android는 즉시 재개, iOS는 권한 제스처 필요라 재탭 1회. ③**자정 자동 리셋** `checkDayRollover`/`startMidnightWatcher`(30s 폴링) — days[]가 날짜별이라 새 날 0부터, 열린 채 자정 넘으면 표시 갱신, 과거는 주간바 보존. ④안내문구 정직화(자동재개·자정리셋·플립 한계 명시).
3. **검증(eval)**: tracking 시작=true·영속화·정지=false, resume→motionActive 재개, 롤오버 갱신, 오늘/어제 독립(500/9999), 콘솔 에러 0. 실센서·플립은 실기기에서만.

### 2026-06-28 — 소통 "인기 검색어" 라벨 명료화 + 칩 클래스 충돌 분리
> 사용자: "검색하면 왜 '전체 #키워드'가 나오지요?" (소통→인기순의 인기 검색어 칩이 설명 없어 혼란)
1. **원인/설명**: 그 "전체 #키워드 N"은 **소통 탭 '인기순' 정렬의 인기 검색어 칩**(§9 확정 스펙) — 상단 검색창에서 코스를 찾을 때마다 `searchCounts` 누적, 탭하면 그 키워드 후기만, '전체'=필터 해제. 버그 아님.
2. **변경**: ①`renderCommKeywords`에 **`.kw-lead` 라벨**("🔥 인기 검색어 · 상단 검색창에서 많이 찾은 키워드, 탭하면 그 후기만") 추가로 자명화. ②**클래스 충돌 분리**: 상세 '출발 전 체크'의 준비물/검토 칩이 소통 인기칩과 같은 `.kw-chip`을 공유 → 상세 쪽을 `pc-chip`/`pc-block`/`pc-head`/`pc-wrap`로 개명(디커플). ⚠️ **교훈**: 신규 컴포넌트 클래스명은 기존과 겹치지 않게(공용 prefix 충돌 주의).
3. **검증**: 인기순 라벨 노출, 상세 준비물 12칩=pc-chip(잔여 kw-chip 0), 콘솔 에러 0. (주의: python http.server 프리뷰는 브라우저 캐시가 stale될 수 있음 → `location.replace('/index.html?cb='+Date.now())`로 캐시 우회 검증.)

### 2026-06-28 — UX 5종: 맛집검색·사진확대·트레킹카드축소·로고홈링크·상태칩 2배
> 사용자: ①선택검색서 "맛집" 검색 안 됨 ②사진 터치 확대 ③헬스 트레킹 카드 4개 아이콘 삭제+세로 축소 ④좌상단 "꽁아코스" 로고 홈 링크 ⑤만보기 "목표까지 N보" 상태칩 폰트 2배.
1. **변경**: ①`FOOD_SYN`(맛집/먹거리/음식/식당…) → `getFilteredCourses` 검색이 음식 동의어면 `hasFood(c)`로 확장(맛집 8→217). ②`openPhotoZoom(el)` 라이트박스(전체화면 확대+X닫기, bg-image/IMG 모두 지원) → `.gphoto`(상세 갤러리)·`.fc-photo`(소통 인증샷) 클릭 연결(피드는 stopPropagation). ③`trek-card`에서 `<i>` 제거 + 패딩 축소(12→8). ④`appbar-logo`에 `onclick=goTab('home')`+cursor. ⑤`.status-pill` 11px→**22px**(2배)·`.hero-top` flex-wrap.
2. **교훈**: literal 텍스트 검색은 의미검색이 안 됨 → 핵심 동의어는 테마술어로 확장 매핑(맛집=hasFood). bg-image 사진 확대는 `style.backgroundImage` 파싱이 깔끔(URL 인라인 인자 회피).
3. **검증(eval/preview)**: 맛집 217·먹거리 217, 라이트박스 overlay on+imgSrc, 로고 onclick=home, 트레킹 아이콘 0/4카드, 상태칩 24.2px, 콘솔 에러 0.

### 2026-06-28 — 상세 스탯행(난이도·소요시간·만족도) 아이콘 제거 + 폰트 확대
> 사용자: 상세의 "난이도 소요시간 만족도" 폰트 조금 크게 + 불필요한 아이콘 삭제.
1. **변경**: `stat-row`의 fa-mountain/fa-clock/fa-thumbs-up 3개 아이콘 제거. `.stat i` 규칙 삭제. `.s-l` 10.5→**12.5px**(색 text-mut·700), `.s-v` 14→**17px**.
2. **교훈**: 정보성 스탯은 아이콘보다 라벨+값 가독성이 우선 — 군더더기 아이콘 제거 + 폰트 한 단계.
3. **검증**: 상세 스탯 아이콘 0, 라벨 13.75px·값 18.7px, 콘솔 에러 0.

### 2026-06-28 — 소통 인기순 해시태그 칩 제거 [이전 스펙 변경]
> 사용자: "방문후기 인기순 탭하면 왜 아직도 해시태그가 있는가?" (앞서 라벨만 달았으나 칩 자체를 원치 않음)
1. **변경**: `renderCommKeywords`를 **항상 빈 칩 바**로(인기 검색어 `#키워드` 칩·라벨 전부 제거). 인기순은 **인기도 정렬만** 유지(`getCommFeed`의 `commSort==='popular'`). `commKeyword`는 항상 ""이라 필터 미적용. `.kw-lead`/`.kw-chip`(소통) 미사용.
2. **교훈/스펙변경**: 앞선 "검색순=인기검색 정렬 + 인기 키워드 칩"(2026-06-28 소통 정정)에서 **칩 UI는 폐기** — 인기순은 정렬만. 사용자가 "왜 아직도 있나"는 = 제거 요청(라벨 추가로는 불충분).
3. **검증**: 인기순 탭 → comm-kw-wrap 비어있음(해시칩 0), 피드 인기순 정렬 유지, 콘솔 에러 0.

### 2026-06-28 — 소통 컴포저 "사각나라" 탈출 리디자인 + 정렬 정확성 검증 (ui-ux-pro-max)
> 사용자(캡쳐 지시): 컴포저(의견입력+사진/갤러리/올리기)가 "사각나라도 아니고, 리디자인". + 최신/평점/인기순 정확 정렬 확인. (해시태그는 이미 제거됨 — 캡쳐는 캐시)
1. **변경(컴포저)**: 테두리 카드+박스입력+3등분 박스버튼 → **테두리 없는 에어리 카드**(soft shadow)+**필(pill) 입력**(border 제거·radius22·focus accent-line 링)+**사진/갤러리 고스트 링크**(무테·투명)+**올리기 단일 강조 필 버튼 우측 정렬**(margin-left:auto). ui-ux 가이드(미니멀 chrome·단일 CTA·focus ring) 적용.
2. **정렬 검증(로직 정확 확인)**: `getCommFeed` — 최신순=date desc, 평점순=rating desc(tiebreak date), 인기순=`itemPopularity` desc(votesUp+댓글*3+만족도+검색매칭*20). eval로 단조감소 확인.
3. **검증**: 최신순 date desc(sorted=true), 평점순 4.7… 비증가, 인기순 292→284 비증가, 컴포저 필/고스트/우측CTA, 콘솔 에러 0.

### 2026-06-29 — 헬스 일/주/월 통계탭 + 회원마당 갤러리 정리
> 캡쳐 지시: (1) 헬스 상단에 일/주/월 통계(그래프+수치), (2) 상세 "다녀온 회원마당"의 빈 사각형(X) 정리.
1. **헬스 히어로 일/주/월 세그먼트** + 기간별 막대그래프·합계. `monthDays()`/`healthStats(range)` 추가.
2. **회원 사진 갤러리** → "회원 사진" 라벨 + `<img>` onerror 자동제거(일부 깨짐=해당칸만/전부 깨짐=영역통째 제거). 사진 0장이면 영역 숨김.

### 2026-07-03 — 엑셀 리소스 추가: 행안부 「가고싶은 섬」 47 + 「리서치 나들이」 15 (바다 칩 통합)
> 사용자 지시: `가고픈곳_리서치추가_20260703.xlsx`의 「가고싶은 섬」·「리서치_나들이_추가」 시트를 탐색 탭 리소스로 추가하되 **중복 제외**, 기존처럼 이미지 적합하게 노출.
1. **변경**: ①`defaultCourses` 242→**304**(id 243~304). 섬 47(시트 내 중복 병합: 연홍도·장도·가우도·이수도·연화도·한산·울릉도 / 기존 중복 제외: 반월·박지도(퍼플교 id143)·삽시도·관매도·홍성 죽도) + 리서치 15(둘레길+천년사찰+수목원·동물원+맛집 타임라인). ②**`photoKey` 필드 신설** — `photoKeyForCourse()` 최상단에서 `c.photoKey` 우선 반환(섬=sea 고정, 강천섬=river처럼 제목 '섬' 오탐 방지). ③섬 노출은 취향 8종 칩(P1)의 **`바다` 칩과 통합** — `hasSea()` 최상단에 `c.photoKey==="sea"` 확정 판정 추가(연륙교 진입 섬(교동도·임자도) 누락 방지). 별도 island 칩은 P1 8종 스펙과 충돌해 폐기. ④신규 type `섬 나들이`/`나들이 코스`(검색 "섬" 도달성). 부팅 시 `courses.length!==defaultCourses.length` → 자동 리셋으로 기존 사용자에게도 반영.
2. **교훈(영구)**: 엑셀 리소스 추가 절차 = **시트 판독→기존 242 대비 정확명칭+구성명소 중복검사(시트 내 중복도 병합)→스키마 매핑(season은 관광자원·추천시기에서 추론)→photoKey 명시→검증**. 제목에 '섬'이 없어도(○○도) 섬 코스는 photoKey:"sea" 명시가 확실. 데이터 삽입은 스크립트 replace 대신 **CRLF 주의**(멀티라인 replace 실패 → Edit 도구 사용).
3. **검증**: node --check·validate.mjs PASS, preview 375px — 바다 칩 필터에 신규 섬 포함+실사 바다 사진, 청산도 슬로길 상세(타임라인 4·승선권/물때 준비물 자동), 사찰탐방 86→103(리서치 사찰 연계 포함), 지역필터 도달(전라/경남) 정상, 콘솔 에러 0.

### 2026-07-04 — 모음집 5종 화면 + 카드 세로 규격 통일 + 관리자 사진 교체 (캡쳐 지시, ui-ux-pro-max)
> 캡쳐 2장+지시: ①신규 카테고리 탭을 홈 노란박스(상황 칩)에 추가+정확 검색+선택(탐색) 탭 반영 ②카드 취향 뱃지를 상단 위치줄(원형 지시 위치)로 이동, 전체 카드를 작은 쪽 규격으로 통일(세로폭 낭비 금지) ③관리자 모드 "사진만 교체" 확인/구현 ④무관 이미지 수정 ⑤엑셀 시트 5종(명산100·가고싶은 섬·세계 트레일·골프장 순위·세계 탐방지) 탭 추가.
1. **변경**: ①**COLLECTIONS 모음집 5종**(명산100 100·가고싶은 섬 59·세계 트레일 11(시트 기재분만)·골프장 150·세계 탐방지 192) + `screen-collection` 화면(순번+이름+메타 hairline 행, 그룹 배지, 목록 내 검색). 진입 = 홈 노란박스 칩 5종(SITUATIONS `coll:` prefix) + 탐색 상단 `coll-strip`. 국내 자료 탭 = 섬→코스 상세 직행/그 외→탐색 정확 검색(`collTap`), 세계 자료 = 열람 전용. ②**카드 규격 통일**: `cc-prefs`(취향 뱃지)를 제목 아래 별도 줄→**상단 위치줄 인라인**, `.cc-title` 1줄 클램프, `.cc-main{min-height:calc(90px*--fs); justify-content:space-between}` → **304카드 전부 121px 단일 높이**. ③**관리자 모드**(기존에 없었음→신설): 만보기 ⋮ 설정에 토글, ON이면 상세 대표사진 스트립(160px 전 코스 동일 규격, 신설)에 「사진 교체/기본 복원」 — `photoOverrides`(localStorage `gongacourse_photo_overrides`, 640px JPEG 축소 저장, 카드·상세 모두 최우선 적용). ④**무관 이미지**: `photoKeyForCourse` 폴백을 계절 전에 **타임라인 지형 스캔**(sea/lake/valley/forest/river/park/mountain, 사찰 제외 §4)으로 보강.
2. **교훈(영구)**: 카드 세로 규격 = **뱃지·부가정보는 별도 줄 금지(상단 줄 인라인) + 제목 1줄 클램프 + min-height 단일화**. 신규 목록형 화면은 코스 카드가 아니라 **hairline 행 리스트**(순번+이름+메타)가 규격. 사진 교체는 정적 SPA 한계상 **이 기기 localStorage에만 저장**(서버 없음) — 정직 고지.
3. **검증**: node --check·validate PASS, preview 375px — 홈 칩 11종·모음집 5종 진입/검색/탭 연결(마니산→탐색 1건, 청산도→상세 직행), 카드 304개 단일 높이 121px, 가로 오버플로우 0, 관리자 토글→사진 교체/복원/카드 반영, 콘솔 에러 0.

### 2026-07-04 — 세계 3종 상세페이지 353곳 전수 리서치 + 카카오맵 완전 제거 + 홈 칩 개명 (캡쳐 지시)
> 캡쳐 3장: ①홈 칩 "장거리 트레킹"(2줄 꺾임=옥에 티)→"먼길 트레킹" ②섬 목록·상세 템플릿 호평(유지) ③세계 트레일·골프장·세계 탐방지도 행 탭→외달도와 같은 템플릿 상세 페이지(웹리서치로 내용·이미지), 노란 도형(카카오맵 버튼) 삭제=모든 페이지, **카카오맵 연동 완전 제거**, 제거 후 하위 설명란 상승. 리서치 깊이 확정 = **353곳 전부 웹리서치**(AskUserQuestion).
1. **변경**: ①`screen-collitem` 신설 — 세계 3종 행 탭→`renderCollItem`(detail-hero+사진스트립 160px+스탯 3칸+상세 소개+방문 팁, 코스 상세와 동일 템플릿). 관리자 사진 교체를 컬렉션 항목에도 확장(override 키 `key:gi:ii`). ②**353곳 전수 웹리서치**: 병렬 서브에이전트 11개(트레일11·골프W 3분할·골프K 2분할·탐방지 5분할)가 dd(소개)/tips/stats/ik(영문 이미지키워드) JSON 산출 → 스크립트 병합(353/353, 명칭 불일치 0, 금지어·느낌표 0). ③**카카오맵 완전 제거**(스펙 변경 — 2026-06-28 "식당 전화→카카오맵 링크" 결정 폐기): map-btn·food-card 링크·kakaoMapLink() 전부 삭제, 맛집 카드는 무링크 정보 행. ④SITUATIONS "먼길 트레킹" + `.sit-chip span{white-space:nowrap}` 가드.
2. **교훈(영구)**: ①대량(수백 건) 콘텐츠 리서치 = **입력 청크 파일 + 병렬 에이전트 + 파일로 JSON 회수 + r키 병합** 패턴(에이전트 세션한도로 끊겨도 파일은 완성돼 있을 수 있으니 **파일 무결성 검사로 판정**). ②칩 라벨은 2줄 꺾임 금지(nowrap 가드). ③프리뷰 screenshot 행이면 stop→start 1회, 그래도 행이면 **eval 지오메트리 검증으로 대체**(§5 폴백, 콘솔 0 필수).
3. **검증**: node --check·validate PASS, eval — 커버리지 353/353(dd·ik·stats), 5그룹 표본 상세(스탯·팁 사실 정확: 안나푸르나 5,416m 토롱라·ACAP 허가 등), 카카오맵 DOM 0건+prep 상승, 먼길 트레킹 1줄, 골프 150행 탭 가능, 뒤로가기·여름 추천·가로 오버플로우 0·콘솔 에러 0.

### 2026-06-29 — 만보기 탭 전면 리디자인(참조앱 스타일) + 프로필 간편등록 [핵심 컨셉]
> 캡쳐+반복 교정 지시. 만보기를 "걸음 기록형"에서 "**라이트(화이트) 카드 · 한 화면 · 널찍한** 트래커"로 전면 개편. **이 컨셉을 이후 화면 전반의 기준으로 삼는다.**
1. **만보기(mypage) 카드 = 라이트 컨셉(§4 신규 절대규칙으로 승격)**: 초록 풀배경(=「초록 나라」) **금지** → **화이트 surface + 하어라인 + soft shadow**, 초록은 **포인트로만**(발자국 아이콘·그래프 선/면·진행바·CTA). 일/주/월 세그먼트(라이트 트랙, on=초록) + ⋮ 메뉴(수정·공유·설정 시트). `◀ 날짜 ▶` 이동(과거 조회·미래 차단·오늘로).
2. **히어로 = 발자국 아이콘(초록) + 큰 숫자만**. "걸음" 글자 라벨 **금지**(이중설명). 숫자 **상·하 여백 넉넉히**(margin 30/28). 통계칸(칼로리/거리/시간/속도)은 **아이콘 없이** 숫자+라벨, 세로폭 콤팩트.
3. **시간대 누적 그래프**: Y축 **K(천보) 눈금 + 가로 그리드**, X축 **시(0~24시)**. 초록 선+12% 면, 앰버 목표 점선. SVG `preserveAspectRatio=xMidYMid meet`(라벨 왜곡 방지).
4. **한 화면 가득 + 널찍**: 일 뷰 카드 `.ped2.fill { min-height: calc(100dvh - 140px); display:flex; column }` + `.p2-grid{margin-top:auto}` → 숫자는 상단, 통계·그래프·버튼은 **하단까지 내려** 배치. (사용자 확정: "널찍한 게 좋다")
5. **실제 세션 측정**: 시작~중지 경과시간·평균속도 1초 갱신, 시간별 버킷 누적. **"시작" 한 번 = 자정 넘겨 매일 24h 자동**(롤오버 시 세션 자동 재시작·일단위 리셋).
6. **프로필 간편등록**: 프로필 아이콘 탭 → 모달(필명+휴대폰[로컬만]+사진). `NICK` 동적화. **프로필+필명은 상단 우측 앱바에만**(본문 중복 스트립·등급줄 전면 금지 → §4).
7. **검증**: `node --check` 통과·콘솔 0·일/주/월·⋮·모달·날짜이동·소통/헬스 프로필·Y축K/X축시·한화면 fit(카드 bottom 744<탭바) 실캡처.

### 2026-07-03 — 탐색 취향 태그 검색 P1 (칩 8종 다중선택 AND) [docs/OPTIMIZE.md P1]
> 지시: OPTIMIZE.md P1 프롬프트대로 취향 태그 검색 구현(감지 5종 복제 → 칩 8종 AND → 동의어 → 카드 뱃지 → 0건 완화).
1. **감지 5종**: `hasWaterfall/hasValley/hasSea/hasLake/hasForest` — `spotIsTemple` 패턴 복제한 공용 `spotHasTag/courseHasTag`(토큰 단위 WORD 매칭 + BAD 오탐 배제). 제목+동선(timeline spot)이 태깅 원천, 데이터 수작업 0.
2. **바다 접미 보강(교훈)**: "인천삼형제섬길"(…섬**길**)·"신도항"(…**항**)이 초판에서 누락 → 섬 접미에 `(길|투어|여행)?` 허용 + `○○항`(2자+) 접미 추가(공항·운항·저항 등 BAD 배제). "대관령 소나무 숲"의 바다 태그는 오탐 아님(동선에 해안선·심곡항 실존) — **동선 기반 태깅은 제목과 달라도 정상**.
3. **취향 칩 8종**: 쉬운산책·맛집·사찰·폭포·계곡·바다·**호수/숲(hasLake∨hasForest 결합 1칩)**·장거리. `currentThemeFilters` 배열 **다중선택 AND 교집합**(`matchesTheme` every). 칩 토글 시 **서랍 안 닫음**(연속 선택), 탭 라벨 "계곡 외 1" 요약. 3탭+수납장 패턴 유지.
4. **동의어**: FOOD_SYN 패턴 재사용 — WATERFALL/VALLEY/SEA/LAKE/FOREST/TEMPLE_SYN. 전역 검색 "폭포"→hasWaterfall 전체 매칭.
5. **카드 취향 뱃지 최대 2개**(`coursePrefTags`, PREF_TAGS 순서 고정) + **0건 완화 제안**: 해제 시 결과가 최대가 되는 태그 1개를 `.relax-btn`(44px)으로 제안.
6. **검증**: node --check·375px·콘솔 0. 건수 폭포9·계곡12·바다24·호수/숲80·사찰86(242 유지, 폭주 없음), AND 계곡+맛집=9, 바다+폭포=0→완화버튼 동작. PR #12.

### 2026-07-17 — 홈 「지금 뭘 찾으세요」 국내 폭포 100 + 바로가기 순서 설정 [범위 한정]
<!-- 아래 두 항목은 같은 날 후속 작업 -->
### 2026-07-17 — 폭포 100 상세 실사: 리서치 실사진 22곳 주입 + 한글키워드 폴백 버그 수정 + 출처 표기
> 지시: 국내 폭포 상세의 실사를 점검하고, 리서치로 "실제 그 폭포"의 추천 계절 사진을 넣어라 + 서브에이전트 최대 활용·빨리 정확하게·머지까지 마감.
1. **점검 결과(버그 확증)**: 상세 사진이 loremflickr **한글 키워드** URL이라 전량 defaultImage(무의미 대체 이미지)로 리다이렉트되고 있었음(실측). "해당 폭포 실사"가 아예 아니었음.
2. **변경**: ①`collItemPhoto` 우선순위 = photoOverrides > **it.photo(리서치 확정 실사)** > 키워드 폴백, 폴백은 `collItemFallbackPhoto`가 **영문 토큰만** 사용(한글 키워드 버그 수정), 실패 시 `collPhotoError` 1회 폴백 체인. ②`dp-credit` 사진 출처 표기(작가·라이선스·커먼즈 링크 — CC 귀속 요건). ③`data/waterfalls-part1~5.js` 22곳에 `photo/pc/psrc` 주입(전건 upload.wikimedia.org 직링크·시각 검증 통과분만, 계절일치 14). ④`validate-waterfalls.mjs`에 photo 기계채점 룰(직링크+출처 동반+금지문자) 추가.
3. **리서치 방법(확정 패턴)**: 수집=단일 순차 스크립트 3패스(한글명 검색→kowiki 대표사진→RR 로마자 변환 검색)+카테고리 스윕(300ms 페이싱·UA·백오프·항목별 저장·resume) → 후보 30곳 → **시각 검증**(직접 판독+서브에이전트 3개 분업: 검증A/사냥B/사냥C)으로 22곳 확정, 오매칭 차단(천지연≠천제연, 가평 용추≠제천·함양, 중국 비석·시장열차·워터마크·SVG placeholder 등). **나머지 78곳 = 커먼즈·위키백과에 해당 폭포 실사 부재**(전건 사유 로그) → 영문 키워드 실사 폴백 + 관리자 「사진 교체」로 보완. 무단 대체(다른 폭포 사진) 0건 원칙 유지.
4. **오류원장(제8조)**: ①**병렬 에이전트 10개가 위키미디어 동일 IP 난타 → 429 + 세션한도 전멸(산출 0)** → 수집은 순차 스크립트, 에이전트는 로컬 시각검증·예외 사냥만(전역 memory `mass-webapi-research-pattern`). ②node fs에 `/c/...` POSIX 경로 금지(`C:\c\` 오배치) → C:\ 절대경로. ③MediaWiki pageimages의 파일명 키는 `pageimage`(단수) — `pageimages`로 읽으면 File:undefined 전멸. ④커먼즈 카테고리명은 "Waterfalls **in** South Korea"(of 아님). ⑤`iiurlwidth=640` 요청에도 960px 버킷 thumburl이 반환될 수 있음 — 임의 640 URL 조립 금지, API thumburl 그대로. ⑥동명 폭포(용추·구룡·용소 등)는 지역 교차검증 필수, zone 파생 지역토큰이 폭포명 스템과 같으면 가드 무력화되므로 제외. ⑦서브에이전트 가용성은 추정 금지 — **직접 시도로 판정**(추정 답변으로 사용자 재지시 유발 = 제5조 위반이었음). ⑧프리뷰 screenshot/렌더러 행 반복 → stop→start 1회, 그래도 행이면 eval 지오메트리·로드 검증으로 대체(§5).
5. **검증**: node --check·validate.mjs·validate-waterfalls.mjs PASS. 375px eval — 정방폭포 상세 = 위키미디어 실사 **실로드(naturalWidth 960)** + 출처줄("사진 Jocelyndurrey · CC BY-SA 4.0 · Wikimedia Commons"→커먼즈 링크), 홍룡폭포(미확보) = 영문 키워드 실사 폴백 + 출처줄 없음(정상), 콘솔 에러 0, 가로 오버플로우 0.

### 2026-07-17 — 홈 바로가기 6종 = 테마 허브 화면 (B안 구현 완료, ui-ux-pro-max)
> 사용자 지적: 「지금 뭘 찾으세요」 하위 탭들이 산책·맛집·먼길 전부 같은 내용 → 테마별 구성(추천+세부 설명)으로 개선. B안 승인 후 빌더 서브에이전트 구현 + 리뷰 에이전트 + 실화면 검증.
1. **실측 진단(원인)**: 오늘 인기·새로 등록 = `gotoSituation()`이 필터·정렬 없이 탐색 이동 → 304 전체 동일 화면. 먼길 295/304·맛집 233·산책 218로 판정이 느슨하고 정렬이 전 탭 동일(등록순) → 상단 카드가 항상 같음.
2. **구현(확정 스펙)**: 신규 `screen-themehub` + `THEME_HUBS` 6키 + `renderThemeHub()` — 키별 고유 [필터·정렬·행 메타]: 산책=쉬움·3h·짧은순(27) / 맛집=2곳+·많은순·대표상호(204) / 사찰=hasTemple(spotIsTemple 경유)·인기순·사찰명(103) / 먼길=4h+·긴순(67) / 오늘 인기=itemPopularity 인기순(304) / 새로 등록=id 최신순+NEW 배지(304, 62개). 상단 **오늘의 테마 추천 1개 = 반드시 현재 계절 풀 ∩ 테마**(§4, 풀 공백 시 카드 미노출) + 날씨 보정(pickTodayCourse 동일식) + 근거 1줄. 리스트=모음집 hairline 행 규격(coll-row 재사용, 순번+이름+테마 메타), 하단 「탐색에서 조건 바꿔 더 보기」(easy/food/temple/long만, `gotoExploreTheme()`=기존 취향칩 라우팅 보존). 신규 클래스는 th- 프리픽스.
3. **검증**: node --check·validate 2종 PASS. 375px — 6키 상단3 전부 상이, 추천 6건 전부 여름 풀, 뒤로가기→홈, 더보기→탐색 temple 103건, 행 66px(44+), 가로 오버플로우 0, 콘솔 0. 홈·탐색·컬렉션 출력 불변(diff 확인).
4. **오류원장(제8조)**: ①python http.server 프리뷰에서 index.html 신규 섹션이 안 보이면 **브라우저 캐시**(신규 화면 root null → 빈 화면) — `location.replace('/index.html?cb='+Date.now())` 캐시 우회 후 판정(2026-06-28 처방 재확인). ②**정렬 비교자 안에서 O(n) 조회(courses.find) 재계산 금지** — 리뷰 에이전트 FAIL 지적: 304행 인기순 정렬이 itemPopularity ~5,000회 호출 → `popCache`(렌더당 1회 전체 계산) 후속 수정, 재렌더 25ms 실측. ③날씨 재조회 콜백이 재렌더를 부르면 위치 거부 시 무한 루프 — 세션당 1회 가드(`renderThemeHub.weatherTried`) 필수(홈·헬스 동일 패턴은 별도 후속 권장).
> 사용자 지시: 앱의 다른 구성요소는 수정하지 않고 오직 「지금 뭘 찾으세요」 탭 작업만 수행. 맛집 코스 우측에 「국내 폭포」를 추가하고, 각 바로가기를 개인 취향대로 이동해 첫 화면에서 볼 수 있게 설정.
1. **변경**: ①`Top100_전체` 100곳을 홈 전용 `WATERFALL_COLLECTION`으로 분리(기존 `defaultCourses` 304개 무변경), 계절 4그룹(봄8·여름45·가을43·겨울4), 목록·기존 컬렉션 상세 템플릿에 접근/운영·선정근거·HTTPS 출처까지 표시. ②SITUATIONS 기본 순서 `가볍게 산책→맛집 코스→국내 폭포→사찰 탐방`. ③「순서 설정」 바텀시트: 12개 바로가기 위/아래 이동·첫 화면 배치·기본복원, 상위 4개 표시, `visitorSettings.homeShortcutOrder` 로컬 영속. ④국내 폭포는 홈 전용이라 탐색 모음집 스트립에는 노출하지 않음.
2. **범위 규칙**: 홈 추천·동반자·후기, 탐색·소통·헬스·만보기는 출력 불변. 특정 구성요소 한정 지시는 해당 DOM·스타일·진입 경로만 수정하고 diff hunk+실제 화면으로 인접 구성요소 불변을 증명한다.
3. **검증**: `node --check app.js`·`node scripts/validate.mjs`·`node scripts/validate-waterfalls.mjs` PASS. 375px: 첫 4개 폭 320px<가용 347px, 순서 설정 12행·터치 44px·새로고침 영속·기본복원, 폭포 목록 100행·정방폭포 상세·HTTPS 출처, 탐색 스트립 국내 폭포 0, 다른 홈 3구성 존재·페이지 가로 overflow 0·앱 콘솔 오류 0.

### 2026-07-19 — 국내 폭포 100곳 실제 사진·AI 재현 이미지 전수 정비 [승인·범위 확정]
> 사용자 지시: 작업 전 계획 승인, `AGENTS.md`에 세부 작업을 먼저 기록, 서브에이전트 병렬 Workflow 필수, **폭포 탭 외 수정 금지**, 인터넷에서 폭포별 자료를 하나씩 확인, 운영자 사진 교체 설정은 다른 탭까지 확인, 완료 후 요구사항 매칭 검증과 PR 머지·라이브 배포까지 마감. 시작 시각 `2026-07-19 09:40:08 KST`.
1. **허용 범위**: `AGENTS.md`의 본 지시 기록, 국내 폭포 컬렉션 데이터(`data/waterfalls*.js`), 폭포 이미지 자산·검수 원장, 폭포 전용 렌더 분기와 `scripts/validate-waterfalls.mjs`만 변경한다. 홈의 다른 바로가기·추천·후기, 탐색·소통·헬스·만보기, 일반 코스, 다른 컬렉션, `defaultCourses`는 변경 금지이며 diff·해시·375px 화면으로 불변을 증명한다.
2. **병렬 Workflow**: 서브에이전트 3개가 순위 `1~34`, `35~67`, `68~100`을 분담해 인터넷 근거·실제 사진 후보·AI 재현용 사실 프롬프트를 작성한다. 이후 담당 범위를 교환해 교차검수하고, 메인 에이전트가 100곳 전부를 2차 승인한다. 동일 에이전트의 단독 조사·최종승인 금지.
3. **실제 사진 승인 기준**: 기존 22곳도 예외 없이 재검증한다. 폭포명·시군구 또는 좌표·고유 형상, 원본 설명 페이지, 저작자·라이선스/공공누리 유형을 확인해야 한다. 동명이 다른 지역, 주변 산·계곡, 일반 폭포, 블로그·SNS·지도 무단 사진, 출처 불명 이미지는 반려한다. 승인본은 원격 랜덤 핫링크가 아니라 프로젝트 로컬 자산으로 저장하고 출처를 상세에 표시한다.
4. **AI 재현 기준**: 정확한 재사용 실제 사진을 찾지 못한 폭포만 공식 설명·위치·지질/절벽·단수·소·주변 구조물·추천 계절·유량 상태를 근거로 포토리얼 재현 이미지를 개별 생성한다. 다른 폭포 사진을 해당 폭포처럼 복제하지 않으며, 파일·화면·alt·원장에 `AI 생성 재현 이미지 · 실제 촬영 사진 아님`을 표시한다. 생성 프롬프트와 근거 URL을 보존한다.
5. **전수 원장**: 순위, 정식명·동명이 구분, 시도/시군구(공식 확인 시 좌표), 공식 사실 URL, 시각 식별표지, 원본/설명 URL, 저작자·라이선스, 실제/AI 유형, 1·2차 검수자, 반려사유, 로컬 경로, 원본/결과 SHA-256, 생성 프롬프트, 화면 크레딧 문구를 100곳 모두 기록한다.
6. **운영자 설정**: 기존 `만보기 → ⋮ → 설정 → 관리자 모드`와 `gongacourse_photo_overrides`가 폭포·일반 코스·컬렉션 상세에 작동하는지 확인만 한다. 이번 범위에서는 다른 탭의 공유 관리자 로직을 변경하지 않는다. 폭포 상세에서 교체→새로고침 유지→기본 복원→기본 이미지·출처 복귀를 검증한다.
7. **완료 게이트**: `검증된 실제 사진 + 명시된 AI 재현 이미지 = 100/100`, 일반 랜덤 폴백 0, 유형·출처·원장 누락 0, 폭포별 매칭 100건, 다른 탭 변경 0이어야 한다. `node --check app.js`, `node scripts/validate.mjs`, `node scripts/validate-waterfalls.mjs`, `git diff --check`, 375px 전수/표본 화면·콘솔 0·가로 overflow 0을 통과한 뒤 독립 리뷰→커밋→푸시→PR→머지→Pages 재빌드→라이브 자산까지 확인한다.

### 2026-08-16 — 시드 콘텐츠 표기 정직화 + 기본 브랜치 푸시 차단 훅

**지시 맥락.** `C:\dev` 7개 사이트 PainPoint 조사(기록 `C:\dev\검증기록\2026-08-16_전사이트_페인포인트\`)에서 이 저장소 항목이 **A등급**으로 확정됐다. 홈 "방금 올라온 후기" 3장이 지명만 바뀐 같은 문장이었다(청산도/방축도/송이도). 주인님 결정 = **라벨 정직화** — 문장 다양화는 가짜 후기를 더 그럴듯하게 만드는 일이라 채택하지 않았다.

**판독.** 원인은 렌더러가 아니라 **콘텐츠의 정체**다. 후기 700건이 시드이고 필명은 10개(상위 3명이 611건), 최신 날짜가 2026-06-27(당시 50일 전)인데 필명·날짜·별점을 달고 사람 글처럼 노출됐다. 소통 탭 "최신순" 상위 10건은 전부 같은 필명·같은 날짜였다. 정렬이 그렇게 굳는 이유는 시드에 `ts` 가 없어 `feedTs` 가 날짜로 폴백하고 동률에서 안정 정렬이 코스 번호순을 남기기 때문이다 — 그 함정은 `app.js:22365` 주석이 **이미 알고 있었다.** 알면서 시드를 폴백 경로에 방치한 것이 이번 결함이다.

**처방.** 데이터에 이미 구분 수단이 있었다 — 사용자가 등록한 글만 `ts`·`uid` 를 남긴다. 그 구분을 화면에 반영했다. `isUserWritten()`·`dedupeByPhrase()`·`hasUserReviews()` 신설 → 홈은 실제 후기가 있으면 그것만 "방금 올라온 후기"로, 없으면 시드를 "코스 한 줄 소개"로(필명·별점 제거 + 문장 중복 제거) · 소통 피드·코스 상세는 시드에 필명 대신 "코스 소개", 날짜·별점 미표시. **내용은 하나도 지우지 않았다 — 바꾼 것은 그것이 무엇인지 밝히는 표기다.** 실제 후기가 1건이라도 등록되면 표기가 자동으로 후기로 돌아간다(로컬에 남아 있던 테스트 후기 1건으로 그 경로도 확인).

**게이트(§10 기계 채점).** `scripts/visitor-gate.js` J1축에 4항 신설 — 홈 후기 섹션 제목 측정 · 문장 서로 다름 · 제목이 내용과 일치 · 시드에 사람 표기 없음.
> **오류 원장(제8조)**: 첫 작성 때 제목 선택자를 `.sec-label` 로 써서 heading 이 빈 문자열이 되며 룰이 **조용히 통과**했다(실제 마크업은 `sectionLabel()` = `.sec-t`). 선택자를 고치고 "제목 측정"을 **별도 항목**으로 세워 못 찾으면 그 자체가 FAIL 이 되게 했다 — 측정 실패를 통과로 흘리지 않는다.

**검증.** 로컬(처음 온 방문자 상태)과 라이브 둘 다: 제목 "코스 한 줄 소개" · 카드 3장 문장 3개 전부 고유 · 메타는 코스명만 · 소통 피드 상위 6장 필명 "코스 소개"·날짜/별점 없음 · 코스 상세 동일 · 게이트 4항 PASS · 옛 제목 뮤테이션에서 FAIL 확인 · `node --check` · `validate.mjs` PASS. 배포 `132352f` → Pages.

**절차 위반과 그 처방.** 이 커밋을 §8-5 를 어기고 master 로 **직접 푸시**했다. 조문이 문서에만 있고 채점하는 것이 없었다. → `.githooks/pre-push` 신설(기본 브랜치 푸시 exit 1) · 설치 한 줄을 §8-5 에 기재 · 그 훅 자체를 PR #26 으로 올려 머지했다. 실제 커밋으로 차단을 확인했다. 같은 위반이 CanaryLab 에도 있어 동일하게 처리(PR #31). **이미 배포·검증이 끝난 코드는 되돌리지 않았다** — 동작하는 배포를 흔들 뿐 얻는 것이 없어 절차 구멍만 막았다.

### 2026-08-13 — 방문자 관점 전수 실사 2차: 잔여 가지 31건 (게이트 22종 전량 통과)
> 1차(뿌리 5개) 이후 잔여 57건 중 상위 31건을 적용했다. 서브에이전트 7기(별 파일 2 + app.js 직렬 5) 완료 시점에 **주인님 지시로 중단**하고 검증·머지했다.
1. **내비**: 탭 전환이 히스토리를 쌓지 않게(replace) · `screenHash()`로 pushState에 URL 전달(코스 공유·새로고침 복원) · `scrollMem` + `scrollRestoration="manual"`로 뒤로가기 스크롤 복원 · `closeAllOverlays()`/`pushOverlayState()`로 오버레이 3종 뒤로가기 대응 · `showScreen` 백지 방어.
2. **성능**: 탐색 20장·소통 20장·테마허브 페이지네이션("더 보기") · 검색 렌더 디바운스 260ms · 소통 인기순 비교자 내 전수 재탐색 → `courseById` Map 캐시 · 만보기 세션 타이머 누수 정리.
3. **후기 흐름**: 투표가 상세를 통째로 재렌더하던 것 → `updateVoteUI()` 부분 갱신(작성 중 초안·별점 보존) · 내 후기 삭제 버튼 · 사진 디코딩 실패(HEIC 등) 처리 · 후기 사진이 소통 피드에 노출 · 정렬용 `ts` 분리 · 별점 필수 해제 · 프로필 사진 타입·용량·오류 가드.
4. **접근성**: 탭 타깃 12종 중 10종 44px 확보 · viewport `user-scalable=no`/`maximum-scale` 제거(확대 허용) + iOS 자동 확대 방지로 입력 글자 16px · `<noscript>` + app.js 로드 실패 폴백(재시도 버튼) · today-card·feed-item에 `role="button"`+키보드 핸들러.
5. **천책빵**: 주 경계 복귀 시 재추첨 경로(`redrawIfWeekChanged`) · 셔플 편향 제거(LCG → mulberry32) · `weightByReading`이 1번 자리에서 선호를 배제하던 문제 교정.
6. **검증**: `scripts/visitor-gate.js` 16종 → **22종**으로 증설. **22/22 PASS**. 탭 히스토리 4→0 · 탐색 304→20장 · 소통 103,130→3,142자 · 스크롤 600→600 복원 · 만족도 96%→96% 불변 · 터치 타깃 0/31 · 대비 0/28 · 375px 오버플로 0. `defaultCourses` 304개 무손상(변경 시작 20,488줄 = 데이터 영역 밖).
7. **보류 1건**: G3-16(후기 영역 이모지) — CSS·HTML로는 손댈 지점이 0이고 app.js 템플릿 문자열 안의 텍스트라 담당 밖. 다음 차수.
8. **미착수(다음 차수)**: C4 데이터 정합(G1-04 버전키+후기 병합 · G3-11 만족도 변형 · G22-18 필명 기반 지표 · G22-03 필명 변경 시 삭제권), C5 빈 상태·모음집·죽은 코드, C6 접근성 잔여(별점 radiogroup · 필터 서랍 aria-expanded · 설정 접근 경로), C7 이스케이프 전수·문서 정합. 약 26건.
9. **원장**: 이번 차수 중단은 소요 초과가 원인이다. **"전부 발굴"을 "전부 수정"으로 확장한 것이 근본 오류** — 발굴 범위와 수정 범위를 처음부터 분리해 산정해야 한다.

### 2026-08-14 — 방문자 관점 전수 실사 3차: 잔여 27건 종결 (게이트 29종·뮤테이션 증명)
> 전날 세운 계획서로 착수. 결과 원장은 `docs/PLAN-2026-08-13-잔여27건-작업계획.md` 가 단일 원천이다 — 여기에 항목표를 복제하지 않는다.
1. **변경**: Agent 3기 직렬(구역 배타 소유) + 루트 직접. 19건을 고치고 8건은 이미 해소돼 있어 재구현하지 않았다. 신설 게이트 7종을 `scripts/visitor-gate.js` 에 붙여 **29/29 PASS**.
2. **교훈 ① — 계획서는 하루 만에 낡는다.** 27건 중 **8건이 착수 시점에 이미 구현돼 있었다.** 원인은 잔여 목록에 처리 결과가 역반영되지 않은 것이다. **처방(영구): 서브에이전트 프롬프트에 「생존 대조 먼저」를 박는다** — 항목을 고치기 전에 현행 코드로 결함이 아직 살아 있는지 판정하고, 해소된 것은 재구현하지 말고 근거(함수명·줄 번호)와 함께 `이미 해소` 로 반환하게 한다. 이번에 이 한 줄이 8건의 헛일을 막았다.
3. **교훈 ② — 통과만 보는 게이트는 구멍을 숨긴다.** 신설한 「필명 이스케이프」 게이트가 **공허하게 통과**하고 있었다. 화면에 뜨는 이름은 `NICK` 인데 게이트는 `visitorSettings.nick` 만 바꿔, 렌더가 옛 값을 그려 아무것도 재지 못했다. **뮤테이션(처방만 되돌리기)이 이걸 잡았다** — 되돌렸는데도 초록이면 그 게이트는 규칙이 아니라 메모다. **처방(영구): 신설 게이트마다 처방만 되돌리는 뮤테이션을 돌려 「수치로」 죽는지 확인한다.** 파일을 통째로 옛 버전으로 되돌리면 추출·마커에서 먼저 죽어 증명이 되지 않는다.
4. **교훈 ③ — 게이트가 홈 화면만 잰다.** 터치 타깃·대비 검사가 홈 기준이라, 만보기 화면의 38px·34px 버튼이 22/22 초록을 통과하고 있었다. 이번엔 사람이 손으로 찾았다. **화면별로 재는 것이 다음 차수 1순위다**(계획서 §3).
5. **§4 위반 1건 적발·수정**: 헬스 '트레킹 활동' 카드가 `내 후기 N` 을 노출하고 있었다. §4 는 등급·후기수 표기 **자체**를 금지한다(2026-06-29 재지적). 카드와 그 집계용 전수 스캔을 함께 걷었다.
6. **데이터 실측 정정**: 문서 5곳이 코스 수를 `242개·1~17373줄` 로 적고 있었다. 배열 리터럴을 파싱해 실측한 값은 **304개·5~20456줄**(id 1~304, 중복 0). 이력 기록의 242 는 그 시점 사실이라 그대로 뒀다 — **낡는 것은 현재 상태를 값으로 말하는 문장이다.**
7. **검증**: 29/29 PASS · 가로 오버플로 0 · 콘솔 에러 0(favicon 404 제외 — `index.html` 에 아이콘 링크가 없어 브라우저 기본 요청이 난다) · `node --check` · `validate.mjs` PASS · 뮤테이션 3종 전부 수치로 실패.

### 2026-08-13 — 방문자 관점 전수 실사 1차: 뿌리 5개 절단 (§10 헌법 제정 + 기계 게이트 신설)
> 주인님 지시: 방문자 관점 Test로 불안정·편의성 개선사항 전수 발굴 · MECE 검토 후 세부 작업항목을 AGENTS.md에 헌법으로 등재 · 서브에이전트 병렬 Workflow 필수 · 계획 승인 후 착수 · 매칭 검증 후 머지까지 마감. 계획서 `docs/PLAN-2026-08-13-방문자실사-작업계획.md`.
1. **발굴**: 라이브 실사(Playwright 375px)로 증상 19건 확보 → 발굴 Workflow 5기가 90건 보고 → 중복 제거 72건(치명 7·높음 24·중간 28·낮음 13) → 적대 검증 3기가 CONFIRMED 6·PARTIAL 25·REJECTED 0으로 판정.
2. **수정(뿌리 5개)**: ①`lsGet`/`lsSet` 저장소 안전 래퍼 신설 + localStorage 직접 호출 28곳 전량 치환 + 부팅 try/catch + 실패 시 `role="alert"` 고지 배너 ②전역 `escHtml()` 신설 + 필명·후기·최근후기 주입 지점 이스케이프 ③`weatherTried` 가드 + 콜백 비동기화로 위치 거부·오프라인 시 홈 무한 재렌더 차단 ④히스토리 `__exit` 2단 센티널로 종료 확인 팝업 복구 + 프로필 모달 히스토리·ESC 대응 ⑤`pickTodayCourse`에 동행 보정 추가(계절 풀 내에서만 — §4 준수).
3. **접근성**: `--text-mut`/`--text-faint` 대비 상향(2.28→4.5:1 이상), 흰 글씨 배지용 `--acc-deep` 토큰 신설, 터치 타깃 44px 확보(동행칩·아바타·로고·검색·이름 버튼).
4. **검증**: `scripts/visitor-gate.js`(16 게이트) 신설. **기준선 11 FAIL → 최종 0 FAIL(16/16 PASS)**. `node --check` · `validate.mjs` 통과. 치명 1건은 위치 거부+API 차단 상태에서 재렌더 2초 38회 → 2회 정지로 A/B 실측.
5. **오류 원장**: ①`preview_start gonga` 실패 — cwd가 `C:\dev`라 상위 `.claude/launch.json`이 우선. 상위에 `gonga` 등록으로 해결(§10-4). ②뒤로가기 궤적 1차 측정이 비단조 — `history.back()` 간격 160ms로 호출이 합쳐진 계측 artifact. 700ms 재측정으로 정상 확인. ③"저장 후 모달 안 닫힘" 오판 — `closeProfileEdit()`는 DOM을 남기고 클래스만 제거하는데 `.pm-card` 존재로 판정했다. **DOM 존재 ≠ 화면 표시.** ④`sed`로 `localStorage.setItem`을 일괄 치환하다 래퍼 내부까지 바꿔 자기 재귀를 만들었다. **일괄 치환은 정의부를 먼저 제외하고 건다.** ⑤게이트가 숨겨진(`img-failed`) 깨진 이미지까지 세고, 스택 바닥에 못 닿은 채 종료 확인을 "발동 안 함"으로 읽었다. **게이트 실패를 앱 결함으로 단정하기 전에 게이트 자신을 의심한다.**
6. **범위 변경 고지**: 편집 지점을 M=20으로 산정했으나 실측 193개. 시급성 순서(①방문자불편 ②코드결함 ③편의성)에 따라 이번 차수는 뿌리 5개까지로 끊고, 중간 28·낮음 13건은 다음 차수로 이월한다.
7. **미결(다음 차수)**: 오버레이 2종(순서설정·사진확대) 히스토리 대응 · 뒤로가기 스크롤 위치 복원 · 데이터 버전 키(`DATA_VERSION`) 도입 시 사용자 후기 병합 보존 · 탐색 304카드/소통 698장 페이지네이션 · 검색 디바운스 · 후기 수정·삭제 수단.
8. **스펙 확인 요청**: `showScreen`의 프로필 노출을 `mypage|community|saved` → `detail·collitem 제외 전 화면`으로 넓혔다. 2026-06-28 로그의 "내정보에서만 노출"과 다르므로 주인님 확인이 필요하다. 375px 앱바 실측상 레이아웃 붕괴는 없다(스크린샷 확인).

---

## 10. 방문자 관점 실사 헌법 (Visitor Audit Constitution) — 2026-08-13 제정
> 주인님 지시(2026-08-13): "방문자 관점에서 Test하고 불안정·편의성 개선사항을 전부 발굴 · MECE로 검토해 세부 작업항목을 AGENTS.md에 헌법으로 새길 것."
> 근거 계획서: `docs/PLAN-2026-08-13-방문자실사-작업계획.md`. **규칙만 있고 게이트가 없으면 반복된다(헌법 제10조 ⑤) → 모든 축에 기계 채점 게이트를 1개 이상 붙인다.**
> ※ §9는 append-only 로그다. 새 로그 항목은 §9 끝(이 절 앞)에 추가한다.

### 10-0. 판정 원칙
- **눈이 아니라 기계가 채점한다.** 스크린샷 인상평은 판정이 아니다. 판정은 DOM 수치·콘솔 카운트·localStorage 실값 대조로 낸다.
- **시급성 순차 고정**: ① 방문자가 지금 막히는 것 → ② 코드 결함·오류 → ③ 편의성 제공. 이 순서를 뒤집지 않는다.
- **부재도 결함이다.** 기능이 아예 없어서 방문자가 못 하는 일(예: 로그아웃)은 "해당 없음"이 아니라 발굴 대상이다.
- **처방은 불변 제약 안에서만**(§2 빌드리스·localStorage 전용). 서버 도입이 필요한 처방은 별도 승인 사항으로 분리 보고한다.

### 10-1. MECE 4군 11축 — 세부 점검 항목과 게이트

**Ⅰ. 생애주기 축 — 방문자가 겪는 순서 그대로**

| 축 | 점검 항목 | 기계 게이트 |
|---|---|---|
| **J1 진입** | 캐시 0 첫 렌더 시간 · 흰 화면 구간 · 첫 화면 정체성 전달 · 데이터 0건(빈 저장·빈 후기) 화면 붕괴 | 콘솔 에러 0 · `DOMContentLoaded`→첫 카드 렌더 ms 기록 · 가로 오버플로 0px |
| **J2 신원 등록** | 앱바 프로필→간편등록 모달 열림/취소/재편집 · 필명 미입력·공백·초장문·특수문자 · 휴대폰 형식 오류 · 사진 대용량/비이미지 · 저장 후 앱바 즉시 반영 | 저장 후 `gongacourse_visitor_settings` 실값 대조 · 새로고침 후 필명 유지 · 모달 ESC·포커스 트랩 |
| **J3 관심사항 등록** | 동행 4종 · 취향 태그 8종 다중선택 AND · 홈 바로가기 순서 설정 · 되돌리기 · 전체 해제 시 0건 화면 | 선택값→localStorage 값 1:1 · 필터 결과 0건일 때 빈 상태 안내 존재 |
| **J4 맞춤 반영** | 오늘의 추천이 **현재 계절 풀에서만** 나오는가(§4 절대규칙) · 날씨 실패 시 계절 폴백 · 관심사항이 실제 정렬·추천을 바꾸는가 | 설정 전/후 추천 목록 diff ≥ 1건 · 계절 불일치 코스 0건 |
| **J5 재방문 복원** | 새로고침·탭 재개설 후 필명·사진·설정·저장목록 복원 · 구버전 스키마 키 내성 · localStorage 저장 거부(비공개 모드·용량 초과) 폴백 | 저장소 차단 주입 후 콘솔 에러 0 · 화면 렌더 성공 |
| **J6 이탈** | 로그아웃 존부, 없으면 기기 내 데이터 해제 경로 존부 · 종료 팝업 발동 조건·문구·취소 동작 · 화면별 뒤로가기 도착지 · 새로고침 후 위치 | 홈 최상단 뒤로가기 = 팝업 1회 · 하위 화면 뒤로가기 = 팝업 0회 · 팝업 취소 후 화면 유지 |

**Ⅱ. 화면·연결 축**

| 축 | 점검 항목 | 기계 게이트 |
|---|---|---|
| **S1 탭·화면 완결성** | 탭 5종(홈·탐색·소통·헬스·만보기) + 화면 9종(상세·모음집·모음집상세·테마허브 포함) 각각 진입·핵심 조작 1건·빈 상태 | 화면별 필수 DOM 앵커 존재 · 콘솔 에러 0 · 빈 상태 문구 존재 |
| **S2 백링크 왕복** | 모든 진입점(홈 바로가기 6·탭 5·카드·모음집·검색결과)에서 뒤로가기 도착지가 진입 직전 화면인가 · 딥링크 새로고침 후 위치 | 진입점 × 뒤로가기 도착지 표 **전량 일치**(기계 대조) |

**Ⅲ. 품질 축**

| 축 | 점검 항목 | 기계 게이트 |
|---|---|---|
| **Q1 코드 결함·오류** | 콘솔 에러·미처리 예외 · 사용자 입력의 innerHTML 주입 · 네트워크 실패 폴백 · 중복 렌더/경합 · 타이머·리스너 누수 | `node --check app.js` · `scripts/validate.mjs` · `scripts/growth-gate.mjs` · 콘솔 에러 0 · 입력 이스케이프 확인 |
| **Q2 편의성·접근성·성능** | 터치 타깃 44px · 본문 대비 4.5:1 · aria/role · 폰트 3단 · `prefers-reduced-motion` · app.js 1MB 로딩 · 이미지 지연 | computed style 실측으로 44px·4.5:1 위반 0건 · 전송량 기록 |

**Ⅳ. 위생 축**

| 축 | 점검 항목 | 기계 게이트 |
|---|---|---|
| **H1 문서·코드 위생** | md 중복(스테일 worktree 사본 포함) · 문서-코드 불일치 · 죽은 코드 · 앱 미참조 데이터 파일 · 빌드 타임 산물 | md 중복 문자열 검출 0 · 문서에 적힌 줄 번호가 실제와 일치 · 앱 미참조 파일 목록 0 |

**부수 축(별 폴더)** — **X1 천책빵 질문 주기**(`cheonchaekbbang/app.js` 이번 주의 질문): 주가 바뀌면 첫 화면 문장이 **반드시** 바뀌는가 · 같은 주 재방문 시 묶음 유지 · 저장 거부 기기 폴백 · 문항 풀 갱신 이력.
게이트: 주차 키 3주치 주입 후 첫 문장 3개가 서로 다른지 기계 대조.

### 10-2. 견적 산정 규칙 (신설 — 산정 오차 재발 차단)
> 원인: 작업량이 다른 단계에 **Agent당 같은 상수를 일괄로 곱했다**(1차 계획 5시간 43분 → 실제 2시간 9분~2시간 36분).

1. 견적에는 **작업량 실측치**(줄 수·조작 수·편집 지점 수)와 **유도식**을 반드시 노출한다.
2. 단계에 상수를 일괄로 곱하는 산정을 **금지**한다.
3. 유도식 없이 시간을 적은 계획은 **마감 차단 대상**이다.
4. 소요는 Agent 수가 아니라 **⌈Agent÷2⌉ = Batch 회전 수**로 계산한다(이 기계 물리 2코어 → Workflow 동시 실행 상한 2).
5. 편집 단계 견적은 **편집 지점 수 M의 함수**로 적는다. M은 적대 검증 이후에만 확정되므로 그 전 단계는 **확정 구간과 변동 구간을 분리 표기**한다.

### 10-3. 실사 절차 (고정 순서 — 중복 발굴 금지)
1. **라이브 먼저**: Playwright로 방문자 흐름을 완주해 **증상 원장**을 만든다.
2. **정적 발굴은 그다음**: 증상 원장을 서브에이전트 **입력으로 주입**한다. 같은 결함을 정적·라이브로 두 번 발굴하지 않는다.
3. **적대 검증은 라이브 재현 불가 항목만**(경합·폴백·예외 경로). 화면에서 즉시 재현되는 것은 Playwright 실측으로 즉결한다.
4. **회귀는 스크립트로 1회 작성** 후 재실행한다. 회귀를 두 번 수기로 돌리지 않는다.
5. `app.js` 편집은 **직렬 강제**(단일 파일 충돌 100%). `style.css`·`index.html`은 동시 가능.

### 10-4. 도구·환경 확정 사항
- **프리뷰 서버 이름 해석은 상위 `launch.json`이 우선한다.** cwd가 `C:\dev`면 `C:\dev\.claude\launch.json`이 읽히므로 여기에 `gonga`(python http.server 8765, `--directory 꽁아코스`)가 등록돼 있어야 한다. 폴더 안 `.claude/launch.json`만 믿고 `preview_start gonga`를 부르면 실패한다. (2026-08-13 실측)
- 서버 기동 후에는 **문자열 대조로 신원을 확인한 뒤** 판정한다(title에 `꽁아코스`).

